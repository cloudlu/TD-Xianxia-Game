# 仙侠塔防 · 系统设计文档

> 版本：v0.98（2026-09 御剑守城：塔符自动飞入 + 千里剑气，夜袭关可稳定通关）

### ⚠️ v0.98 实现现状（御剑守城）

**设计动机**：v0.96 键盘英雄只改了一半——两套交互（神雷锤击 + WSAD/1/2/3）割裂；且"进关无法移动"暴露 tickHero 只跑 wave 的 bug。v0.97 重做：夜袭 5 关（ch7/12/17/22/27-l2）**彻底无塔**，真人 = 移动的塔，塔符 = 塔能力（与塔技能同语义、可重用、叠加共存），纯鼠标驱动。v0.98 依实机反馈收口：**塔符改为击杀处爆出+自动飞入**（无需走位）、本命飞剑升格**千里剑气**（全图射程）、攻击改**真实弹道**（飞行命中+飘字掉血+爆光动感）并修复**英雄击杀不设 dead 导致砍死也漏**的致命 bug，夜袭关稳定可通且打击感完整。

**核心循环**：移动鼠标 → 真人沿墙线（wallRow-0.5）左右跟位（HERO_SPEED=6 格/s）→ 敌人被击毙处**爆出塔符**（发光玉牌拖尾光束，自动飞向真人入体）→ 获得/升级塔能力 → 各能力按自身射程 + 冷却**发射真实弹道自动开火**（飞行到敌人身上才结算，命中飘字+爆光）→ 点击敌人**集火**（或点空处取消）。

**实现**：
- **纯函数**（`engine/pure/hero.ts`，18 tests）：`createHero`（中列出生，自带本命飞剑 Lv0）、`moveHeroX`（targetX 匀速逼近 + 左右钳制 + 朝向翻转）、`homeSigils`（已激活塔符以 9 格/s 匀速追踪真人，≤0.6 格停止等待入体）、`tryGrantSigils`（到达即自动授予+移除；满槽→回弹上空并延后 spawnAt；过期→移除）、`grantAbility`（剑并入既有升级；新系叠加共存；上限 4 系 + 本命剑）、`tickCooldowns`、`dmgOfSchool`（maxHp×dmgPct×1.4^level）、`planAbilityHit`（集火优先 > 最近；aoe/chain/pierce 多目标）。全部确定性，无随机无时钟。
- **数值规格**（`HERO_ABILITY_SPECS`，与对应塔炼气档同语义，伤害按敌 maxHp 比例）：sword **千里剑气** 35%·1.0/s·射程99（**命中全图范围内全体敌人**，墙线面防守核心，任何一列都吃得满）；talisman 18%·1.5/s·3.0·单体；spear 15%·1.0/s·2.5·横扫（近身带 0.6 至多 3）；fire 28%·0.8/s·2.5·AOE1.2；thunder 30%·0.7/s·2.8·链3跳2.0；ice 12%·1.2/s·2.8·减速50%/2s。
- **Game**：`pureHero` 标志（构造时 buildable 全置 false、canPlace 恒 false）→ `setHeroTargetX`/`setFocus`/`clearFocus` + `tickHero`（移出 waveActive 守卫，prep 期也可移动）每拍：移动→homeSigils 追踪→tryGrantSigils 入体（满槽回弹）→焦点失效清理→冷却→**逐能力发射真实弹道**（从真人发出、按各系形状飞行至命中，`计划 targets 全部 spawn projectile`）→`TowerCombat` 统一结算；**波间保留**（startWave 不再清空能力）。
- **攻击→弹道→命中全管线**（v0.98 动感完整化）：英雄各能力不再"凭空扣血"，改为向每个目标发一枚**真实弹道**（sword 剑气横贯全图扇形飞剑 / talisman 符纸 / spear 枪形 / fire 火球 / thunder 电弧 / ice 冰晶，Board 按 school 渲染剑穗/拖尾分境界形态）；到达目标时走 `TowerCombat.damage` 管线——**命中爆光（系色 hit 特效）+ 飘字掉血数字（0.7s 上浮，暴击金色）+ hitFlash 受击白闪 + 击杀 poof/BOSS 多层爆散 + 冰系命中附加减速**，与塔攻击同一套表现与结算（护甲/闪避/护盾语义一致）。击杀必设 `e.dead=true` 供清理/波次结算。
- **塔符击杀掉落**（v0.98 取代随机布点）：`scheduleStreamWave` 不再随机撒符；改为**敌人被击毙的瞬间于其死亡坐标爆出**（WallRow 内任意深度，掉落后立飞真人），每波预算 3 枚（startWave 置位，6 系种子化等概率抽系）；重复拾同系 → level+1（伤害 ×1.4^level）。
- **站位贴底**（v0.98 依实机反馈）：夜袭关 `stream.wallRow` 覆盖为 `rows-2`——城墙搬到**最后两行**、真人站上城头战线（y = rows-2.5），横扫防线同步 `rows-3`；Board 优先用 `streamWallRow` 渲染城墙（纯英雄 buildable 全 false 时仍画得出），`wallBuildable`/`WALL_INSET` 老城墙布防模型保留兼容。
- **渲染**（Board `drawHeroLayer`）：塔符发光玉牌（系色+icon，临过期闪烁）+ **飞行拖尾光束（虚线流光导向真人）** + 鼠标瞄准竖线/落点光斑 + 真人头顶能力串（冷却暗态 + 等级徽标）+ 集火红准星；弹道与命中特效走既有神雷/塔管线同渲染。
- **输入**（main）：删 WSAD/1/2/3 键盘块；mousemove → `gridXFromClient` 小数格跟位；click → 命中敌人 setFocus / 空处 clearFocus；纯英雄关隐藏塔栏；HUD 显示已拥有塔能力 chips + 操作提示。
- **剧情**：5 关 intro 改写为御剑守城叙事（走位/塔符飞入/集火教学句）。
- **平衡**：真人无敌（能力载体），能力 4 槽上限 + 本命剑常驻 → 可控强度；自动攻击倍率按 maxHp 比例后期不失效；千里剑气保证任意列/时段都能打得满（覆盖优先于数值——无脑追最深的敌人也不漏）。

**验证**：443 tests 通过（+12：hero 纯函数 18 组 + 御剑守城集成 5 组——初始化无塔/横移+塔符自动入体+升级+满槽回弹+波间保留/自动攻击+掉血飘字特效+集火清除/击杀掉落坐标+每波预算封顶/城墙贴底 wallRow 覆盖站位列）。用真实 ch7-l2 配置做**定种子自动驾驶模拟**（chase 与原地两种策略）均 **3 波全清、58/58 击杀、0 漏、满 3 命**（约 60s，弹道飞行引入的到达延迟不破平衡）。历次修复：①v0.97 塔符随机落野战区、真人只横移够不到 → 曾改落巡逻线一行，v0.98 改为**自动飞入**彻底取消走位成本；②单体 22% 吞吐不足 → 全射程全体剑气；③实机打 7-2 仍打不过 → 模拟复现**英雄击杀不设 dead**（负血怪照走、波永不清）致命 bug 已修 + 飞剑升格千里剑气 35%；④v0.98 手感三连调：攻击改**真实弹道**（飞剑/符纸/枪/火球/电弧/冰晶飞行命中）、塔符改**击杀处爆出**、命中附**飘字掉血+爆光+白闪**动感。`npm run build` 通过。

> 版本：v0.96（2026-09 夜袭英雄模式：御剑真人——移动/挥剑/技能符）

### ⚠️ v0.96 实现现状（御剑真人）

> ⚠️ v0.97 已用「御剑守城」重做全部夜袭关输入/掉落/渲染；以下 v0.96 记录保留存史（键盘控制 + 技能符栏已被鼠标走位 + 塔符能力取代）。

**设计动机**：夜袭"建塔→观看"缺少直接操作；把神雷锤击升级为"操控英雄贴身挥剑 + 走位拾取技能"，打小人快感完整化（看到→走位→砸→反馈）。

**核心循环**：WSAD 移动真人（墙外野战区）→ 贴身点击敌人挥剑（≤1.2 格）→ 走位碰撞拾取技能符卷轴（每波 2 枚，三色对应三技能）→ 1/2/3 键释放。

**实现**：
- **纯函数**（`engine/pure/hero.ts`，10 tests）：`moveHero`（匀速 + 斜向不加速 + 野战区钳制，wallRow<1 时退化为全域）、`pickScroll`（碰撞拾取 0.6 格）、`canSwing`（1.2 格）、`nearestEnemy`/`enemiesAround`（天雷/剑气目标选择）。全部确定性。
- **Game**：`hero` 实例（城墙前中央出生）+ `setHeroDirection`（键盘驱动）+ `tickHero`（每帧移动/拾取/剑气节拍）+ `castSkill`（1 天雷：5 格内最近敌 300% maxHp 真伤；2 冰封：全场减速 50% 5s；3 剑气：10s 内真人周围 2 格每 0.5s 一拍 30% maxHp AOE）；挥剑走 strikeEnemy 既有结算（距离校验在 hero 非空时启用）；波间补弹药 + 清空技能栏/剑气/卷轴。
- **掉落**：`scheduleStreamWave` 混入 2 枚卷轴（种子化时刻/位置，只落野战区，60s 过时闪烁消失）。
- **渲染**（Board `drawHeroLayer`，敌人之上）：御剑光弧底座 + 青白袍真人（斗笠+袍摆，朝向翻转）+ 剑身 + 挥剑扇形剑光（0.15s）+ 三色发光卷轴（雷/冰/剑符字）。
- **输入**（main）：WSAD/方向键持续移动（keydown/keyup 集合驱动，斜向归一化由纯函数处理）+ 1/2/3 释放；覆盖层打开忽略；失焦清键防卡键。
- **HUD**：弹药 ⚡ 条 + 技能栏 3 槽（1🌩天雷/2❄冰封/3⚔剑气，亮/暗态）。
- **平衡**：真人无敌（技能载体），但技能栏每波清空 + 挥剑需贴身走位 → 风险与节奏并存；技能战力 ≈ 半座塔，是加分项不破平衡。

**测试**：428 tests 通过（+11：hero 纯函数 10 + 集成 1——移动钳制/贴身校验/拾取/天雷/波间清空）。`npm run build` 通过。

> 版本：v0.95（2026-09 夜袭城墙防线：墙内布防 + 越墙索敌）

### ⚠️ v0.95 实现现状（城墙防线）

**设计动机**：全屏可建下"6 座塔上限"择位张力不足（中线一排塔覆盖所有列）。改为**城墙后布防**：塔只许筑于墙内，敌人压到城墙才进入火力带——守城叙事 + 择位张力双达成。

**实现**（引擎零改动，纯配置 + 渲染）：
- **`streamMaps.ts`**：新增 `WALL_INSET=5`、`wallRowOf(rows)`、`wallBuildable(cols, rows)`——墙行（rows-5）及以上（野战区）不可建，墙下城内 4 行（含防线行）可建 ≈ 40-48 格；`fullBuildable` 保留兼容。
- **越墙索敌**：塔在城内、敌在城外，现有圆形射程判定天然支持，无任何引擎改动。
- **神雷**：不受城墙限制（点哪打哪），补野战区输出真空。
- **横扫符**：仍在城外触发行，语义变为"城门前的最后抓手"。
- **Board 城墙渲染**：从 buildable 矩阵推算墙行（首个可建行的上一行，配置驱动不自欺）；青灰砖渐变 + 三排错缝砖纹 + 顶部等距垛口 + 墙体描边；城外冷暗色罩（荒野感）、城内暖色罩（据点感）。
- **剧情**：5 个夜袭关 intro 各加城墙一句（背城而战/以墙为盾/镇魂墙/联盟界墙/守墙即守道）；同步修正文件头过时注释（"X 道纵向车道"→"全屏随机列出怪 + 城墙布防"）。

**平衡联动**：城内 4 行 × cols 择位空间下，6 塔上限张力回归；妖风变列威胁更大（敌人换列后固定阵位会漏）；冲阵 1.4 速度 + 墙前火力带穿越 ~6s，漏列即扣心——每座塔的位置都重要。

**测试**：417 tests 通过（+1：野战区/墙行不可建、城内/防线行可建）。`npm run build` 通过。

> 版本：v0.94（2026-09 夜袭平衡修复：塔上限 + 冲阵速度 + 敌潮密度）

### ⚠️ v0.94 实现现状（夜袭"1-2 塔通关"平衡修复）

**根因量化**：夜袭敌人全部垂直下行 + 全屏可建 → 2 座错位塔（射程 3 圆）即可横向覆盖全列所有敌人；且夜袭 hpMul 1.7~3.7 偏低（towerMul=sqrt 同步放大塔），实测 1-2 塔通关。直接抬 hpMul 是双刃剑（towerMul 同步涨），故采用**压缩塔有效输出**组合拳：

| 手段 | 参数 | 数学效果 |
|---|---|---|
| **塔数量上限** | `stream.maxTowers = 6`（TowerOperations.canPlace 关卡级封顶） | 直接堵死"2 塔通关"，逼多塔择位（全屏可建下位置选择成为核心策略） |
| **冲阵速度** | `stream.enemySpeedMul = 1.4`（WaveManager 关卡级倍率） | 敌人暴露窗口 7s→5s，塔有效输出 ÷1.4；配合"冲阵"叙事 |
| **hpMul ×1.5** | ch7 2.6 → ch27 5.5 | 净敌强 = sqrt(hpMul) ↑22% |
| **波次密度 ×1.5** | count 取整上浮 | 潮水压力，配合 0~1s 波间 |

**演算验证（ch7-l2）**：改前 2 化神塔（DPS 708）× 30s 窗口 = 2.1 万击杀能力 vs 敌总血 1.4 万 → 2 塔够（证实）。改后：窗口 21s、敌血 2.1 万 vs 2 塔能力 1.5 万 → **不足**；3~4 塔择位 + 神雷 12 发（约 25~35% 波血量）+ 妖风惩罚静态一排塔 → 达成"阵型 + 神雷时机"的双线决策。

**UI**：达上限提示"夜袭阵图限制：最多 6 座塔，择位要紧！"。

**测试**：416 tests 通过（+1：maxTowers 封顶 + enemySpeedMul 位移验证）。`npm run build` 通过。

> 版本：v0.93（2026-09 夜袭 v3：神雷锤击（打小人交互）+ 分辨率自适应修复 + 窄长板型）

### ⚠️ v0.93 实现现状（夜袭手感 + 显示修复）

**背景**：体验反馈两点——①夜袭只有"建塔→观看"，缺少打小人式"看到→砸→即时反馈"的直接操作快感；②夜袭近方形棋盘被 index.html 写死的 `aspect-ratio: 32/12` 强行拉成宽幅，变形严重。

**A · 神雷锤击（打小人核心交互）**：
- **交互**：夜袭关内**点击场上敌人**→天雷锤击：伤害 = 敌人缩放后 maxHp × 30%（与章节 hpMul 同步，后期关不失效），暴击 ×2（精英/BOSS 免暴击），破盾 + 0.8s 震慑减速 50%。
- **弹药制**：每波 12 发（波间补满）+ 0.3s 最小间隔——"省着打精英 vs 倾泻清杂"的策略选择，autoclicker 无利可图。满弹药理论输出约一波总血量 25~35%，是"第三只手"而非取代塔防。
- **即时反馈**：锤击星环 + 8 向星芒 + 砸落锤影特效（`style:'hammer'`）、击杀走既有 cleanup 赏金通道、hitFlash、音效三档（click/realmup0/kill）、每击 1.5~3px 微震屏。
- **连击奖励**：2s 窗口连击计数（HUD ≥5 显示"连击 ×N"），连击 ≥15 且每波一次 → 弹药 +6（高潮奖励）。
- **HUD**：夜袭关显示 ⚡×12 弹药指示器（实心/灰）+ 连击数。
- **确定性**：锤击为玩家输入 + 固定伤害，无随机消费，§1.4 不破。

**B · 分辨率自适应修复（全模式受益）**：
- **CSS**：删除写死的 `aspect-ratio: 32/12`，改双约束 `max-width: 100%; max-height: calc(100vh - 210px)` + `object-fit: contain`——任何板型按内部比例自适应居中，永不变形。
- **DPR 高清**：Board 内部像素 = 逻辑尺寸 × devicePixelRatio（≤2），`ctx.setTransform(dpr,...)` 缩放——逻辑坐标系（格×CELL）对全部绘制代码不变；17 处 canvas.width/height 绘制引用迁移到 logicalW/H；cellAt 输入换算同步修正；main 塔面板定位的 internal/logical 混用修复。

**C · 夜袭板型改窄长**（"横屏宽度小点"）：
- cols 10~12 / rows 16~22（ch7 10×16 → ch27 12×22）——横屏居中比例舒适，防御纵深更长；sweepRow/sweeperCount 随板型调整，waves 配置不动。

**测试**：415 tests 通过（+3：锤击伤害/耗弹/冷却、波间补满、击杀赏金）。`npm run build` 通过。

> 版本：v0.92（2026-09 夜袭 v2：全屏随机列出怪 + 妖风变列 + 全屏布防）

### ⚠️ v0.92 实现现状（夜袭关卡重设计）

**背景**：v0.91 夜袭关复用固定 3-5 条纵向车道，路径缎带割裂全屏、视觉呆板；升级为"全屏出怪 + 面防守"。

**核心改动**：
- **全屏随机列出怪**（`streamMaps.ts` 重写）：paths = **每列一条**纵向直线（pathIndex == 出生列，14-20 条），敌人出生列由种子化 PRNG 决定（`Game.streamSpawnPathOverride` 在 spawn 回调前置拦截，确定性可复算）；现有移动/漏怪/清波模型零改动。
- **全屏可建**：`fullBuildable` 矩阵，整棋盘自由排阵，无路径格限制——玩家从"堵点"转向"布面"。
- **出生预警**（`pure/stream.ts` + Board）：每个出生点提前 1s 在其列顶部画红色聚气光柱（`spawnWarnings` 确定性时刻表，[warnAt, spawnAt) 窗口，收束环 + 渐强光柱）。
- **妖风变列**（本方案点睛）：每 8-12s（关卡配置 `windIntervalSec`）全场存活敌人横移 ±1~2 列——实现为 `WaveManager.retargetPath` 重绑路径（同构同长路径，dist 进度保留、y 不跳变）；`wind` 游戏事件 + Board 横向风纹演出（1.2s 渐隐）；确定性 rng 驱动。破解"单通道堆塔"静态最优解。
- **横扫符按列布点**：防线符从"每车道一枚"改为全列中随机 `sweeperCount` 枚（Fisher–Yates 种子化选取）；触发行判定修正为 `y ≥ sweepRow`（敌自上而下，防线在底部——v0.91 的 ≤ 判定是方向性 bug，本次修复）。
- **表现层**：夜袭关不再绘制路径缎带（`Board.streamScene` 开关），改为防线地平线渐变 + 虚线 + 按列符纸 + 预警光柱 + 妖风风纹的"开阔战场"视觉。

**测试**：412 tests 通过（+12：stream 纯函数 8 + 集成 5 - 旧用例重构，含出生列确定性/预警窗口/妖风变列进度保留/防线符按列拦截/全屏可建）。`npm run build` 通过。

> 版本：v0.91（2026-09 玩法扩展二期：连续流夜袭 + 三路经营战）

### ⚠️ v0.91 实现现状（新玩法模式 P2 + P3）

**P2 · 连续流夜袭（末日塔防式）**：
- **类型**：`LevelMode += 'stream'`、`StreamConfig{sweepRow, prepBetweenSec}`（types.ts）。
- **地图**（`streamMaps.ts`）：纵向车道，敌从上方压入、守下方防线；5 关 `ch7/12/17/22/27-l2`（3→5 道渐进，与 lane 异变关错章）；ch22/27 波间仅 1s，其余 0s（清波即刻开波）。
- **引擎**：sweepCheck 泛化双轴（lane 横向判 x、stream 纵向判 y）；**WaveManager 修复**：横扫判定从"仅漏怪时"改为"越过触发线即扫"（中段防线语义）+ 终点兜底；波间 prep 由 `stream.prepBetweenSec` 驱动。
- **表现层**：Board `drawStreamProps`（青色横扫符，与 lane 金色区分）。
- **设计区分**：夜袭关无灵晶拾取（连续流无波间拾取窗口），核心压迫感来自"无喘息 + 纵向纵深布防"。

**P3 · 三路经营战（打小人式：两侧经营 + 中路打怪）**：
- **类型**：`LevelMode += 'forge'`、`ForgeConfig{forges, forgeSecPerLevel, collectDmgPctPerLevel, maxTotalDmgPct, mergeEnabled}`、`ForgeState`（types.ts）。
- **纯函数**（`engine/pure/forge.ts`，10 tests）：`initForges/tickForges/forgeAt/forgesAdjacent/mergeForges/capForgeBonus`——无随机无时间源，进度由引擎注入 dt。
- **引擎**：Game 持有锻炉状态；`clickForge(uid)` 状态机（就绪→收取伤害符 / 未就绪→选中→相邻同级→合并升级释放格子）；`tickForges` 在波间与波内**持续推进**（经营节拍：波间经营、波内打怪）；锻炉格 `canPlace` 拦截建塔；伤害符经 `CombatUpdateCtx.forgeDmgBonus` **加法并入** effectiveStats 的 dmgMul（与 aura 同池，不绕族封顶）。
- **表现层**：Board `drawForges`（丹炉三足剪影 + 进度环 + 就绪火光呼吸"收"字 + 等级星 + 合并选中白框）；main 点击路由优先锻炉。
- **关卡**（`forgeMaps.ts` + `ch14/23/30-l2`）：4→6→8 炉渐进；经济基准：10s 出符、+5%/级、封顶 +60%（对齐攻速射程档，不破伤害族天花板）；合并两级 = 时长换价值（14s 出 +10% 符），"数量 vs 深度"取舍。

**测试**：400 tests 通过（+15：forge 纯函数 10 + stream/forge 集成 5，含纵向横扫/合并全流程/伤害注入数值验证）。`npm run build` 通过。

> 版本：v0.90（2026-09 玩法多样化：路径原型库 + 车道防守异变关）

### ⚠️ v0.90 实现现状（玩法多样化两阶段）

**背景**：量化诊断两大可玩性痛点——①30 章仅 6 种蛇形路径模板（形状签名全同）；②多路关卡路径长度差最高 90%（ch26 短路 10 格 vs 长路 19 格），破坏 §7.2 核心公式 `塔DPS × 路径覆盖时间 ≥ 敌人血量` 的跨路一致性。

**Phase 0 · 路径地基修复**：
- **等长铁律**：`ConfigLoader` 新增多路长度校验——`(maxLen-minLen)/minLen ≤ 10%`，超标报 error（启动即失败）；`mode:'lane'` 关卡豁免（车道天然等长）。新增校验测试。
- **7 种路径原型**：`tools/genChapterMaps.mjs` 生成器（种子随机 blocked，输出 chapterMaps.ts）——蛇形(33³)/十字汇聚(20/20/21，短臂梳齿补齐)/Y分流合流(31³)/回环(66/66/67)/双桥渡河(35³)/窄道迷宫(84/84/86)/螺旋(53/53/51)。**30 章相邻原型不重复**（旧版同模板连用 4-6 章）；折返"梳齿"为合法死胡同设计；宗门方向组保留（右/左/中心）。

**Phase 1 · 车道防守异变关（PvZ 式）**：
- **类型**（`types.ts`）：`LevelMode = 'classic'|'lane'`、`LaneConfig{sweepCol, pickup{value,intervalSec,maxPerWave}}`、`PickupStone` 运行时状态。
- **纯函数**（`engine/pure/lanes.ts`，7 tests）：`buildLanePaths`（等长直线车道）/`shouldTriggerSweep`（触发线判定）/`pickupSchedule`（种子化确定性掉落时刻表）/`pickupExpired`/`makePickup`。
- **引擎接线**：`WaveManager.update` 新增可选 `sweepCheck` 回调（漏怪判定前拦截，`sweptEnemies` 事件携带被扫敌人）；`Game` 持有每道横扫符状态（每活跃道 1 次/关）、拾取调度（`startWave` 生成计划，帧内到时落地、10s 超时消失防囤积）、`collectPickup(uid)` 点击拾取 API；`sweep` 游戏事件（音效复用 wave sting）。
- **表现层**：Board `drawLaneProps`——横扫符（金符纸呼吸光晕/已用残痕两态）+ 灵晶拾取（菱形灵晶+呼吸圈，<3s 闪烁警告）；main.ts 点击优先判定拾取命中。
- **5 个异变关**（`ch5/10/15/20/25-l2`，`laneMaps.ts` 数据驱动）：章节结构变为 **l1 经典 → l2 异变车道 → l3 BOSS 终局**；3→5 道渐进（ch5 教学 3 道 → ch25 五道雷浪）；敌人从右侧直线压入、守左端宗门；被横扫敌人发小额赏金（计入 economy 遥测）；l2 关名加"·异变"后缀 + 异变主题剧情。
- **平衡护栏**：横扫符=每道 1 次/关不可购买（保底下限，不改写 3 心制——触发瞬间在场敌人被清，紧随者照常扣心）；拾取收益 8-16/颗、每波 ≤4-6 颗封顶（≈波赏金 15-25%）；掉落位置/时刻全走种子化 PRNG（`seed ^ 0x5f3759df` 派生），确定性红线不破。

**测试**：385 tests 通过（+10：lanes 7 + Game 车道集成 3 + 等长校验 1，含确定性/拦截语义/一次性消耗/收集幂等）。`npm run build` 通过。

> 版本：v0.89（2026-09 卡通化主题：色阶提亮 + Q 版表情 + 主题开关）

### ⚠️ v0.89 实现现状（卡通化）

**架构**：`src/data/config/theme.ts` 纯函数（色阶映射 + 主题持久化）+ Board 按主题分支渲染 + `body.cartoon` CSS 主题。**引擎零改动**（纯表现层，确定性红线天然不破）。

- **主题开关**：HUD "🎨 卡通/🖌 写实" 按钮，localStorage `xianxia_theme` 持久化，默认 cartoon；`applyThemeDom` 切 `body.cartoon` class 驱动 CSS
- **色阶映射（`toon()`）**：hex→HSL→亮度钳 0.45-0.70（暗部大幅提亮）、饱和 ×1.35（钳 0.85）、**色相不变**——30 章氛围保留（ch2 血月→糖果红、ch4 森林→抹茶绿）；带透明度特效色原样跳过
- **Q 版萌系（drawEnemy）**：胖椭圆体 + 2px 深描边 + **Q 版大眼**（眼白/瞳孔/高光，瞳孔正弦张望、相位驱动）+ 精英怒眉 + BOSS 獠牙 + 普通怪腮红；走路颠簸 squash & stretch（`scaleY=0.93+0.07·sin(dist·6)`）
- **塔/地形**：圆角塔身（roundRect+描边+高光）、翘檐描边；圆石高光/棒棒糖树/浅蓝圆池白波浪
- **棋盘**：格子/路径色过 `toon()` 提亮；cartoon 路径加描边缎带；vignette 0.45→0.15；moodLight alpha 减半
- **CSS**：`body.cartoon` 糖果按钮（圆角 12px+底部厚度边）、卡片超圆角、星级摇摆动画

**全量测试**：374 tests 通过（+6：toon 提亮/钳制/色相保持/透传/往返稳定性）

> 版本：v0.88（2026-09 剧情旁白 TTS 语音化：角色声线分派 + 打字机同步）

### ⚠️ v0.88 实现现状（剧情旁白 TTS）

**架构**：`src/audio/narrator.ts` 纯函数模块（声线表 + 台词解析，可单测）+ AudioManager `speakLines/stopNarration`（Web Speech API 排队朗读）+ storyModal 接线。

- **角色声线分派（数据驱动）**：`VOICE_PROFILES` 按 `StoryBeat.chapter`（说话人，去空格匹配）分派 gender/pitch/rate——掌门 0.7 威严 / 师叔 0.8 慈和 / 林清霜 female 1.15 清亮 / 神秘人 0.6 沙哑 / 魔尊·魔帅 0.5-0.55 阴沉 / 天道 female 1.3 非人感；未知角色回退旁白（男声 0.9 说书人）
- **台词解析（`parseNarrationLine`，格式约定版）**：**有声书式混排**——台词行（行首/行尾「」，角色声线）与描述行（旁白声线）**全部朗读**，对白段中间的描述（如"掌门负手立于山巅…"）不再跳过；句中名词/书名引用用『』（12 处旧文案已修正）；朗读文本去空白与引号。**narratorAudit lint 测试**保证全部剧情文案遵守约定、全 beat 逐行可读无跳行
- **交互**：跳过/取消/主按钮/静音/切后台均 `speechSynthesis.cancel()`；打字机保留，解锁仍跟随文字完成（语音独立排队可打断）；HUD "🗣 开/关" 开关（localStorage 持久化，默认开）；调试面板"重播对白"入口（无视已读标记全量可选）
- **降级**：无 zh 音色浏览器静默跳过；Chrome voiceschanged 异步加载一次性监听 + 500ms 兜底；女声不可得时回退高 pitch 男声

**已知限制（TTS 路线固有）**：本地中文音色通常仅男/女各一，角色区分度靠 pitch/rate；真正"一人一声优"待第二步配音文件阶段（见 TODO）。

**全量测试**：356 tests 通过（+7：声线映射/台词解析纯函数）

> 版本：v0.87（2026-09 视听升级：色板统一 / 灵气光照 / 动态混音 / 情绪演出）

### ⚠️ v0.87 实现现状（视听升级三批次）

**新增文件**：`src/data/config/palette.ts`（统一色板）+ `src/ui/boardFx.ts`（Board 全屏特效层，SRP 拆出）

**批次1·作品感+紧张感**：
- **色板统一**：灵气色阶（tier0 青 / tier1 金 / tier2 紫金，对齐境界档）+ 妖气色阶（普通/精英/BOSS）+ MOOD_LIGHT 状态光照色；`spiritColor/fiendColor` 纯函数
- **灵气光照**：`drawMoodLight()` 按状态全屏色温（prep 蓝青/wave 暖/boss 红/won 金/lost 灰暗），BOSS 存活时自动升级为 boss 光照
- **动态混音**（AudioManager）：`setBattleIntensity(敌数)` >15 自动压音乐 20%；`startHeartbeat()` lives≤1 低频心跳（55Hz 双脉冲）；`duckMusic()` sting 让位

**批次2·情绪曲线**：
- **时段色温**（保守方案，不换底图）：`dayPhaseTint()` 波次进度 晨蓝→正午→暮橙 插值（alpha≤0.06）
- **最后一波仪式**：`Game.finalWaveAt` 透出；`drawFinalWaveRite()` 2s 水墨暗转 + "妖潮将至"大字
- **漏怪反馈**：`Game.lastLeakAt` 透出；`drawLeakVignette()` 0.6s 边缘红晕心跳脉冲（Canvas 层，与既有 DOM leakFlash 叠加）
- **音画礼让**：realmup1/2 sting 音乐 duck；`visibilitychange` 切后台 suspend 音频

**批次3·打击感细节**：
- **连杀演出**：kill 事件 2s 窗口计数（表现层），≥5 触发 `zoomPulseScale()` 微 zoom（1.05，与震动互斥）+ "N 连斩" DOM 飘字 + 音调递增（已有）
- **塔 idle 动画**：符塔飘符字/雷塔电弧游走/冰塔霜雾呼吸/剑塔环光（相位驱动）
- **击杀音分档**：kill/kill_elite（金属 clang）/kill_boss（低音 sting）三档

**明确不做**（留待音频资源阶段）：主题旋律母题、乐器音色升级、章节专属天气。

**全量测试**：349 tests 通过（+7 palette/dayTint/zoomPulse 纯函数用例）

> 版本：v0.86（2026-08 战力平衡修复 A+C：境界按章解锁 + 仙魂并入族封顶）

### ⚠️ v0.86 实现现状（战力平衡修复）

**背景**：实测账号（VIP15 大 R / VIP7 小 R）均"单塔满级通关"，量化根因——单塔深度成长 83× ≫ 敌人成长 ~10×，深度路线完胜铺塔，§4.1 S3 张力失效。

**方案 A：境界按章解锁（防单塔碾压）**：
- **`levels/index.ts`**：`realmCapForChapter(chapterId)` 分档表——ch1-4 金丹 / ch5-9 元婴 / ch10-14 化神 / ch15-19 渡劫 / ch20-24 大乘 / ch25+ 飞升；合并时注入 `maxTowerLevel`（复用引擎既有 cap 逻辑）
- **豁免（`Game.ts` 构造器）**：① `level.id === 'endless'` 无尽模式不封（终局玩法保留）② 新增 `realmCapExempt?` 回调——已通关关卡复刷不封（`levelSelect.ts` 传 `cleared` 查询），保护 90 关挑战玩法与大 R 复刷体验
- **UI**：hover 卡境界封顶时显示"本关境界上限·金丹（推进章节解锁元婴）"
- 设计意图：章内保留"养塔 vs 铺塔"取舍；ch10 单塔上限化神 DPS 272 vs hpMul 1.9-2.4，必须 3-4 塔协同

**方案 C：仙魂并入伤害族封顶（管道一致性）**：
- **`Modifier.ts`**：`damageStatsFor()` 追加 `'_soulMul'`——仙魂乘数与 VIP/装备/天赋伤害加成**同池加法合并、共享 +150% 封顶**
- **`TowerCombat.ts`**：`effectiveStats` 删除独立 `soulShardMul()` 相乘（原先绕过族封顶，大 R 42810 碎片 = +54% 失控）
- 效果：0 氪 +15% 不受影响；大 R 总伤回归族封顶；仙魂"不封顶"字面承诺保留（边际收益递减而非硬帽）

**遥测验收（§14）**：新增 `recordLevelClear` 埋点——通关时记录 `vipLevel / towerCount / maxTowerLevel / elapsed / livesLeft`；验收指标 = 按 VIP 分层的"塔数<3 通关占比"（单塔依赖度），预期补丁后显著下降。

**暂缓（待遥测数据决策，见 TODO）**：方案 B 压平 L5-L7 曲线（1.7×→1.4× + 过渡章 hpMul -10% 缓冲）——一次只动一个变量，A 上线观察一周后复核。

**全量测试**：342 tests 通过（30 files，+10：境界封顶/复刷豁免/无尽豁免/仙魂族封顶/配置注入单调性）

> 版本：v0.85（2026-08 辅助塔特效 + 震地雷爆炸演出）

### ⚠️ v0.85 实现现状（2026-08 聚灵阵 + 震地雷特效）

**聚灵阵（辅助塔）特效四件套**：
- **常驻呼吸法阵**（Board.ts）：双圈反转虚线弧（外顺/内逆时针），圈层 = 境界档（tier+1），呼吸亮度 sin 相位；中心太极纹慢转
- **射程淡虚线常显**：#81c78433 半透明，无需 hover 即知覆盖范围
- **buff 链接线**：聚灵阵 → 范围内被增益塔，alpha 0.15 极淡虚线 + 沿线相位流动光点
- **受益塔绿环标记**：被增益塔底旋转虚线绿环（与皮肤光晕色系区分）
- **开阵脉冲**：`Game.startWave()` 对每个 aura 塔 push 绿色 shockwave（表现层，无 shake）

**震地雷特效**：
- **待机埋雷标识**（Board.ts）：半埋深色雷体 + 埋土边缘 + 引信红点相位闪烁（tier 越高越快）+ 触发范围土色暗圈常显；开火瞬间引信白热 + 光晕
- **crater 爆炸样式**（新增 hit 样式，earth 流派专属）：按实际 `aoeRadius` 画弹坑（暗色填充+土色描边）+ 6 条锯齿放射裂纹 + 尘土外环 + 8 块土块抛物线飞溅（相位推算，无随机）——**修复了 earth 流派原先不消费 radius 的缺陷**
- **爆炸震屏**：`DamageVis.shake` 透传（MineStrategy 按 tier 3/5/7px），hit 特效携带 shake 标记由 Game.update 统一触发

**架构**：`DamageVis` +`shake?: number`；`TowerVisualSpec.hit` +`'crater'`；全部表现层相位驱动，引擎数值零改动。

**Bug 修复（视觉弹道同帧失效）**：扫射/溅射/链电的伤害即时结算会把目标在同帧标记 dead，原 `updateProjectiles` 只找活目标 → 视觉弹道（dmg=0）一出生即被清除，**枪塔等塔的弹道轨迹完全不可见**。修复：`ProjectileR` +`destX/destY`（目标最后位置），视觉弹道允许飞向尸体/最后记录位置后消散；真实伤害弹道（dmg>0）行为不变。

**全量测试**：332 tests 通过（+1 视觉弹道存活回归用例）

> 版本：v0.84（2026-08 弹道形状化 + 多命中弹道可视化）

### ⚠️ v0.84 实现现状（2026-08 弹道形状化）

**弹道实体形状化（Board.ts 渲染，按 towerVisuals.trail 查表派发）**：
- **剑形**（blade）：菱形剑身 + 剑柄横档 + 剑尖朝飞行方向；tier2 带相位摆动剑穗
- **符纸**（paper）：米黄纸体 + 朱砂三点符文 + 相位飘动残影
- **枪形**（beam）：枪杆细线 + 三角枪镞 + 红缨摆动 + 光带拖尾
- **六棱冰晶**（ice）：六向冰枝雪花相位旋转 + 淡蓝拖尾
- **火球**（orb）：三层爪形外焰（正弦相位摇摆，tier 越大焰越大）+ 白热核心
- **电弧**（bolt）：保持锯齿折线

**多命中弹道可视化（命中数一目了然）**：
- **`spawnMultiTrails()`**（AttackStrategies.ts 导出纯函数）：PierceStrategy/AoeStrategy/ChainStrategy 对每个被击中的敌人 spawn 一条 `dmg=0` 视觉弹道
- **faint 主次区分**：`SpawnedProjectile.faint` 字段——主目标弹道全亮，次要目标 45% 透明 + 细线宽
- **弹道数上限 MAX_TRAILS=8**（含主目标）：超出部分伤害正常结算但不画轨迹（性能护栏）
- 伤害结算不变（仍即时），弹道纯视觉（dmg=0 走现有追踪系统，命中不重复结算）

**确定性红线**：形状旋转/摆动全部由 `elapsed` 相位驱动（sin/cos），无渲染随机。

**全量测试**：329 tests 通过（+3 用例：pierce 多弹道主次/上限、aoe 扇形弹道）

> 版本：v0.83（2026-08 特效第二/三批：打击感细化 + 大场面演出）

### ⚠️ v0.83 实现现状（2026-08 特效第二/三批）

**第二批·打击感细化**：
- **暴击时间缓滞**：`TowerCombat.damage()` 暴击时 `hitFlash 0.12→0.18s`（1.5×），配合金色爆闪
- **弹道残影分身**：Board 渲染 tier2（大乘/飞升）弹道时沿轨迹后段画 2 个渐隐副本（相位推算，无随机）
- **开火后坐**：`drawTower` 利用 `flashTimer` 衰减做 2px 相位回弹（sin/cos 双轴）
- **AOE 爆炸圈按实际半径渲染**：`DamageVis.radius` 由 AoeStrategy/MineStrategy 透传 → hit 特效 `radius` 字段 → Board ring 样式按 `aoeRadius×CELL` 画爆炸圈 + 内焰渐变填充

**第三批·大场面演出**：
- **波次开场演出**：`GameState.waveStartTime` 透出；Board 在波开始 1.5s 内画入口聚气光柱 + 环形收束 + 妖气粒子（仅激活路径）
- **BOSS 顶部大血条**：Canvas 顶部 60% 宽血条 + 名称 + 狂暴阈值金色刻度线；狂暴时（hp < enrageBelow.hpPct）全屏红光脉冲
- **胜利结算演出**：通关时全塔按放置顺序错峰 0.12s 波浪式 realmup（`life = dur + delay`，Board 跳过 `life > maxLife` = 延迟启动）
- **音效分档**：`realmup0/1/2` 三档突破 sting（短促双音/三音和弦/五音 arpeggio+长尾）；击杀连击 2s 内每杀 +1 半音（封顶 +12）

**架构注意**：`CombatContext.damage` 签名扩展 `vis?: DamageVis`（school/tier/radius），策略层显式传递覆盖 combatContext 默认的源塔信息；`SpawnedProjectile`/`VisEffect` 均为可选字段扩展，向后兼容。

**全量测试**：327 tests 通过（29 test files，+2 AOE 半径/暴击缓滞用例）

> 版本：v0.82（2026-08 战斗特效升级：流派命中特效 + 击杀分化 + 屏幕震动）

### ⚠️ v0.82 实现现状（2026-08 战斗打击感三件套）

**① 命中特效差异化（按流派查表渲染）**：
- **`src/data/config/towerVisuals.ts`**：`TOWER_VISUALS` 流派视觉配置表（升级特效骨架/弹道样式/命中样式/拖尾倍率/强调色），纯数据驱动，皮肤可直接覆盖；`visualTier(level)` 境界三档（0=炼气~金丹 / 1=元婴~渡劫 / 2=大乘~飞升）
- **`TowerCombat.ts`**：`VisEffect` 扩展 `kind:'hit'|'realmup'` + `school/tier/style/shake` 透传字段；`damage()` 命中时 push hit 特效（携带攻击塔流派/境界），击杀时 push 分化 poof（`style: normal/feather/elite/boss`，BOSS 带 shake 标记）
- **`Board.ts`**：命中 6 种样式（slash 斜切/ink 墨晕/impact 星芒/ring 火环/spark 电火花/frost 冰晶），暴击 ×1.6 放大金色；击杀 4 种（普通圈/飞行羽毛飘落/精英金散/BOSS 三层爆散+径向碎片）

**② 升级突破特效（v0.81 已做，v0.82 补震动）**：
- 升级 push `realmup` 特效（8 种流派骨架：剑环形剑气/符符文盘旋/枪十字光束/阵六边形法阵/火火柱冲天/雷落雷闪白/冰冰晶绽放/雷地地纹蔓延）
- 元婴+（tier≥1）突破额外冲击波 + 屏幕震动（tier2 强度 6px / tier1 3px）

**③ 屏幕震动系统（确定性）**：
- **`src/engine/pure/shake.ts`**：纯函数 `triggerShake`（取最强不叠加）/ `shakeOffset`（正弦相位 × 线性衰减，**无 Math.random，可复算**）
- **`Game.ts`**：`shake` 状态 + update 扫描新特效的 shake 标记触发；snapshot 透出
- **`Board.ts`**：render 时 ctx.translate 整帧偏移
- 触发点：BOSS 登场（6px）/ BOSS 击杀（7px）/ 精英击杀（3px）/ 渡劫+突破（3-6px）

**红线遵守**：所有特效由逻辑帧 `life/maxLife` 相位驱动，渲染层未引入随机；特效参数全在 towerVisuals.ts 配置表。

**全量测试**：325 tests 通过（29 test files，+5 hit/poof 分化 +5 shake 纯函数）

> 版本：v0.81（2026-07 30 章专属路径 + 宗门方向多样化）
> 主题：仙侠 / 修真
> 形态：Web（TypeScript）+ 纯文字简易网格
> 定位：单机优先，预留联网接口

> 版本：v0.80（2026-07 无尽模式跳关 + 阵眼 + 战略深度）

### ⚠️ v0.80 实现现状（2026-07 无尽模式深度改进）

**跳关机制**：
- **`src/engine/EndlessMode.ts`**：新增 `parTime(wave)`（标准通关时长）、`calcSkip(clearTime, wave)`（根据实际/标准比决定跳过 0-3 波）
- **`src/engine/Game.ts`**：新增 `skipWaves(n)` 方法，递增 waveIndex 并发放被跳过波次的清波奖金
- 实战效果：快速清波可大幅缩短无尽模式总时长，跳过波次的奖励和分数正常记录

**阵眼系统**：
- **`src/types.ts`**：新增 `FormationTile`、`FormationType` 类型；`LevelConfig.formations` 可选字段（普通关卡也可使用）
- **`src/engine/EndlessMode.ts`**：`ENDLESS_FORMATIONS` 常量定义无尽地图 9 个阵眼（风眼×2、雷眼×3、地眼×2、灵眼×2）
- **`src/engine/TowerOperations.ts`**：`formationAt(col, row)` 方法；`TowerR.onFormation` 记录塔所在阵眼类型
- **`src/engine/combat/TowerCombat.ts`**：`effectiveStats()` 应用阵眼加成——风眼+1.5射程、雷眼+40%攻速、地眼+50%伤害、灵眼相邻阵眼塔各+15%（叠加）

**波间加持**：
- **`src/engine/EndlessMode.ts`**：`BLESSINGS` 常量（9 种加持定义），`pickBlessings()` 随机 3 选 1
- **`src/app/levelSelect.ts`**：`showBlessingChoice()` 弹窗，每 10 波暂停并展示 3 选 1
- 选择后永久生效于本局无尽模式，`Game.endlessBlessings[]` 记录

**主动技能**：
- **`src/engine/Game.ts`**：`triggerEndlessSkill()`（天雷·灭：全屏 2000 真伤）、`endlessSkillReady` 冷却检测（25 波 CD）
- **`index.html`**：`#endlessSkillBtn` 圆形按钮（右下角浮层），ready 时闪烁金边

**塔上限**：
- **`src/engine/TowerOperations.ts`**：`MAX_TOWERS = 18`，`canPlace()` 新增数量检查

**战报记录**：
- **`src/engine/Game.ts`**：`WaveSnapshot[]` 每波记录（清波耗时/跳关标记/塔DPS/经济），`battleReport` 导出完整战报
- **`src/main.ts`**：调试面板新增战报 tab，运行时显示波次/分数/灵石/加持/塔状态

**全量测试**：272 tests 通过（26 test files）

### ⚠️ v0.81 实现现状（2026-07 30 章专属路径 + 宗门方向多样化）

**30 章路径重设计**：
- **`src/data/config/chapterMaps.ts`**：30 章每章独立设计 3 条正交路径（仅 R/L/U/D），验证通过后写入 `CHAPTER_MAPS`
- **宗门方向按章节分组**：
  - ch1-8：右侧宗门 (x=31)，路径从左/上/下三方向汇入
  - ch9-14：左侧宗门 (x=0)，路径从右/上两方向汇入
  - ch15-19：顶部宗门 (y=0)，路径从底/左两方向汇入
  - ch20-24：底部宗门 (y=11)，路径从顶/左两方向汇入
  - ch25-30：中心宗门 (15,6)，路径从三方向汇聚
- **`src/data/config/levels/index.ts`**：旧 `LAYOUTS`/`LAYOUT_MAP` 后处理更名为 `CHAPTER_MAPS` 合并
- **障碍物**：每章 12 个（6 岩石 + 3 树木 + 3 水域），程序生成并避免覆盖路径
- **全量测试**：281 tests 通过（26 test files），路径边界/正交/无重复点均验证

**变更影响**：
- 旧 `layouts.ts`（60 种布局）保留但不再用于章节关卡；`layoutMap.ts` 保留映射但不影响 `CHAPTER_MAPS`
- 渲染/寻路/生成等引擎逻辑零改动（入口标"入"、出口标"宗"的逻辑不变）
- 宗门坐标移至边缘/中心后，塔放置/Buildable 网格自动适配

### ⚠️ v0.79 实现现状（2026-07 挑战内容落地 + 地图布局多样化）

**挑战内容落地**：
- **`src/data/config/challengeConfig.ts`**：90 关每关 2 个挑战（速通 + 流派/禁升级/禁光环/预算），共 180 个挑战定义
- **`src/data/Registry.ts`**：`level()` 自动从 challengeConfig 合并挑战到 LevelConfig
- **挑战种类覆盖**：速通(15-100s)、单流派(剑/枪/法/符/阵)、禁升级、禁聚灵阵、预算限额
- **挑战奖励**：贡献值（`rewardContrib`），与设计文档 §8.3 一致
- **挑战触发**：复刷已通关关卡时该关全部挑战同时开启（无需选择），通关逐个判定、达成几个算几个（v0.80 简化，移除选挑战弹窗）

**地图布局多样化**：
- **`src/data/config/layouts.ts`**：60 种独特路径布局（之前 17 种），覆盖左→右/右→左/上→下/下→上/混合共 5 类方向
- **`src/data/config/layoutMap.ts`**：90 关映射表，每种布局最多 3 关（之前三路平行重复 31 关）
- **`src/data/config/levels/index.ts`**：自动根据 layoutMap 覆盖路径 + 重算 buildable
- **入口/出口不再固定**：敌人可从左/右/上/下四边出现，宗门（出口）也随之变化，渲染自动适配（`path[0]=入`, `path[-1]=宗`）
- **单关路径数不变**：l1=1路 / l2=2路 / l3=3路 的章节设计保留
- **零引擎改动**：spawn/dist/leak/targeting 均不依赖方向

**全量测试**：253 tests 通过（26 test files）

### ⚠️ v0.78 实现现状（2026-07 VIP 天命阶系统完善）

**天命阶（VIP）系统完善**：
- **15 级 VIP 体系**：凡人 → 天命一阶~十五阶，累计充值仙玉解锁（60~22000 仙玉）
- **累计加成走 Modifier 管线**：赏金/攻速/全伤/暴击/射程，与装备/天赋/皮肤/仙魂同池合并，**按 stat 分级封顶**（伤害+150%/经济+80%/攻速射程+60%/暴击≤0.6），**付费不破封顶**
- **运营功能**：每日奖励（贡献/天命符/仙魂）、每周奖励、升级一次性礼包、功能解锁（2×自动战斗/跳过波次/离线挂机/额外天命符位）
- **业务函数**：`claimVipDaily/claimVipWeekly/claimVipOneTime/upgradeVipTo`（含幂等、日期/周 key 防重复）
- **IAP 联动**：充值发放仙玉后自动校验并批量升级 VIP（`upgradeVipTo`），同步刷新 VIP 标签页
- **UI 重构**：`metaVip.ts` 独立文件，含当前特权、每日/周/礼包领取、下一级预览、15 级进度条
- **遥测埋点**：`recordVipStatus` 上报 VIP 等级、实时有效伤害/赏金/攻速乘子、是否触顶，供平衡分析
- **测试**：新增 `vip.test.ts`（10 tests）、`progressMeta.test.ts` 新增 18 tests，全量 242 tests 通过

**全量测试**：242 tests 通过（24 test files）

### ⚠️ v0.77 实现现状（2026-07 仙魂商店 + 转生 UI 完善）

**仙魂商店（不封顶永久加成）**：
- **`Progression.soulShopLevels: Record<string, number>`**：各项购买等级，默认空
- **`SOUL_SHOP_STATS` 配置**（`app/metaSoulShop.ts`）：10 项可购永久加成
  - 6 大流派伤害（剑/枪/弓/法/体/盾）：每级 +1%，基础 100 碎片，每级 +50，上限 50 级
  - 攻速：每级 +0.5%，基础 200 碎片，每级 +80，上限 40 级
  - 灵石获取：每级 +0.5%，基础 200 碎片，每级 +80，上限 40 级
  - 攻击射程：每级 +0.1 格，基础 300 碎片，每级 +100，上限 20 级
  - 暴击率：每级 +0.5%，基础 300 碎片，每级 +100，上限 40 级
- **构建器集成**：`buildMods()` 读取 `soulShopLevels`，按 `perLevel` 生成 `mul_pct` modifier，走同一 Modifier 管线，与装备/VIP/天赋/仙魂独立乘数叠加
- **UI**：宗门面板新增"仙魂商店" tab，显示当前碎片数、独立乘数（√碎片×0.8%），列表可点购买，实时刷新

**转生 UI 完善**：
- 选关顶部进度栏显示：`仙魂 +X.XX%`（实时计算 `sqrt(shards)*0.008`）
- 转生确认弹窗：展示获得碎片数 + 当前总碎片 + 转生后独立乘数

**全量测试**：214 tests 通过（22 test files）

**视觉升级**：
- **敌人轮廓多样化**：普通→圆形、飞行→菱形、精英→五边形、BOSS→六边形 + 名称标牌 + 光环底
- **塔开火 muzzle flash**：`TowerR.flashTimer`，发射瞬间白色+塔色膨胀光圈（0.12s 衰减）
- **弹道拖尾线段**：对角线渐变线段 + 核心亮点替代纯圆点
- **升级突破金光爆发**：VisEffect kind='burst'，8 条径向金线 + 中心光晕
- **BOSS 登场冲击波**：VisEffect kind='shockwave'，扩张环形 + 填充
- **环境灵气粒子**：`Board.ambientParticles`，缓慢上升淡出光点（prep 生成更快）
- **场景切换淡入淡出**：CSS fade-out 类 0.28s，`requestAnimationFrame` 清除
- **波次进度条**：HUD 行内渐变色进度条显示已杀/总数

**音频升级**：
- **自适应分层音乐**：`AudioManager.setMusicTension('prep'|'wave'|'boss')`，切换音阶/波形/时长/音量/drone
- **BOSS 登场 sting**：低音 sawtooth + sine 复合 blip
- **击杀音变调**：每次 ±1 半音随机
- **胜利号角/失败哀乐**：6 音上行 arpeggio / 下行 minor + drone fadeout
- **UI 点击音**：short sine blip（880Hz 0.04s）
- **音量滑条**：音乐/音效分离，music default 0.5，sfx default 0.9

**交互升级**：
- **全局索敌策略**：HUD 4 按钮统一设置（首敌/末敌/最强/最近），新塔沿用，`TowerOperations.setGlobalTargetPolicy`
- **塔 hover 属性卡**：固定定位 tooltip，显示 DPS/射程/暴击全境界表格
- **3× 速度档**
- **关卡解锁修正**：同难度链式推进 + 难度递进（简单→普通→困难逐级解锁）

**架构变更**：
- `TowerR.flashTimer`：number（muzzle flash 计时器，0 表示无）
- `GameState.waveSpawned` / `waveKilled`：波次进度暴露
- `CombatGameEvent`：'boss' 事件类型
- `TowerOperations.globalTargetPolicy`：TargetPolicy | null

**全量测试**：199 tests 通过（21 test files）

### ⚠️ v0.76 实现现状（2026-07 转生系统）

**转生系统**：
- **`Progression.reincarnationLevel`**：number，转生次数，默认 0
- **`reincarnate()` 纯函数**（`repo/progressMeta.ts`）：需所有 90 关至少通关一次（任意难度）→ 重置 `cleared`/`difficulty`/`endlessBest`/`endlessMilestones`，保留所有 meta（装备/皮肤/VIP/天赋/贡献/玉/抽卡碎片/灵石/法宝等），奖励 `50 + level×30 + totalStars×2` 仙魂碎片
- **判据**：`allLevelsCleared()` 遍历 `ManifestEntry` 列表，每个 `levelId` 在 `cleared` 中至少有一个难度 key（`levelId:difficulty`）即视为已通关
- **UI 按钮**：选关页面顶部显示，仅全关卡通关后可点击；弹窗确认 + 展示奖励

**全量测试**：214 tests 通过（22 test files，+4 reincarnate tests）

### ⚠️ v0.73 实现现状（2026-07 SRP 重构）

**Game.ts SRP 拆分（设计文档 §1.3，多轮 review 后的落地）**：
- **WaveManager**（`engine/WaveManager.ts`，144 行）：拥有 enemies[] 所有权，负责生成/移动/漏怪/清波判定。
- **TowerOperations**（`engine/TowerOperations.ts`，146 行）：拥有 towers[] + stones，负责放置/升级/出售/索敌切换。
- **TowerCombat**（`engine/combat/TowerCombat.ts`，311 行）：拥有 projectiles[] + effects[] + killStack，负责攻击/弹道/伤害/光环/BOSS 技能/撞塔。
- Game.ts 从 562 行降至 211 行（-62%），职责收敛为协调者（Orchestrator）。
- 3 个新测试文件（WaveManager 9 tests + TowerOperations 21 tests + TowerCombat 17 tests），全量 199 tests 通过。

### ⚠️ v0.72 实现现状（以代码为准，覆盖下方部分初版描述）

**v0.72 经济与至宝系统（2026-07-11，目标 500K CNY whale 天花板）**：
- **奖池重写为 8 层**（特等奖 0.01% → 空奖 40%），300 抽硬保底一等奖（不升级为特等奖）。代码 `gacha.ts`。
- **限定至宝 12 件**：每件带独占机制 stat（doubleAtkChance/armorPierce/waveRefund 等），随机法宝 stat 池不存在这些 stat。代码 `limited_treasures.ts`。
- **随机法宝生成器**：6 种数值 stat 池、随机名字/槽位/1-4 条 stat，碎片合成获取（30 碎片）。代码 `equipment.ts:generateRandomEquip()`。
- **装备碎片升级**：成本从贡献改为碎片（`2^level`，合计 1,023 碎片升满 10 级）。代码 `progress.ts:upgradeEquip()`。
- **仙魂独立乘数**：旧 `floor(shards/5)×1% cap 30%` → 新 `1+sqrt(shards)×0.008`（不封顶）。代码 `state.ts:buildMods()`。
- **至宝引擎消费**：Game.ts 新增 7 处 stat 读取点（doubleAtkChance/armorPierce/leakToStone/waveRefund/densityRate/killStackDmg/bossCooldownMul）。代码 `Game.ts`。
- **空奖保底**：40% 空奖给 5 贡献意思一下，不再纯损失。
- **Modifier 管线扩展**：新增 14 个 getter/setter（`ModifierSet`），4 个新 cap 类别。代码 `Modifier.ts`。

### ⚠️ v0.71 实现现状（以代码为准，覆盖下方部分初版描述）

**v0.71 平衡调整（2026-07-11，针对玩家反馈"几百元即满级"）**：
- **仙玉→贡献汇率**：100 仙玉兑 1000 贡献 → **200 贡献**（下调 5 倍），防充值直通满级。
- **仙魂碎片**：独立封顶 **+30%**（150 碎片达上限），不可单独触达全局 +150% 伤害帽。代码 `state.ts:72`。
- **天命符加成**：每张 +15% → **+8%**（单关），削弱囤符碾压。代码 `levelSelect.ts:104`。
- **抽卡返还**：refund 奖品 80 贡献 → **40 贡献**，提高抽卡有效成本。代码 `gacha.ts:52`。
- **抽卡概率与保底**：稀有奖品（soul8 0.6% + destiny3 0.4%）合计 **< 1%**；保底计数从 **9→99**（百抽保底）。代码 `gacha.ts:21-28,34`。
- **寻仙抽奖弹窗**：改为独立覆盖层展示，十连逐项点击查看，稀有奖品金色发光。代码 `metaScreen.ts`。
- **天命符使用确认**：进关前弹双按钮确认框（使用/不用），战斗中 HUD 显示加持指示器。代码 `levelSelect.ts` + `storyModal.ts`。
- **storyModal 双按钮模式**：新增 `ConfirmBeat` 接口，支持确认/取消双按钮。代码 `storyModal.ts`。

### ⚠️ v0.5~v0.7 实现现状（以代码为准，覆盖下方部分初版描述）

以下为**已落地**的实现，与下方 §4/§9 的初版设计有出入时以此为准：

- **塔：7 种**（非 §4.2 的 4 种）：飞剑/符箓/长枪/聚灵阵/火法(aoe)/雷法(chain)/寒冰(slow)，各 8 境界（炼气→飞升）。攻击行为策略注册表含 `projectile/pierce/aoe/chain`。
  - 平衡调整（v0.6）：符箓 cost 70→85 + 升级费上调（55/90/150/250 → 70/110/180/300）+ 炼气射程 3.5→3.0；飞剑 cost 80→70；聚灵阵全境界 buff ×0.75（20%→15% 炼气）。目的：消除符箓"全维度最优"的单解问题，让飞剑在升级性价比上反超。
- **装备：3 槽位**（非 §9.2 单武器槽）：本命法宝(武器)/护体法衣(护甲)/随身灵物(饰品)，**三槽同时生效**；每件可**强化**（+15%/级，最多 5 级）。见 [装备与皮肤设计.md](装备与皮肤设计.md) v2 节。
- **玩家档案：本地多存档隔离**（设计文档未覆盖）：`profiles.ts` 按 profileId 命名空间 localStorage；启动选档案、可切换/删除。
- **货币闭环**：贡献(通关/星级产出 + 仙玉兑换) / 仙玉(充值 mock + 兑换贡献)；充值走 `IAPRepo`（本地 mock，联网换 Remote）。
- **敌人 35 种 / 机制 14 种**已实现：飞行/护盾/受击回血/高甲/撞塔/隐身(聚灵阵破隐)/分裂/BOSS 周期技能(魅惑+召唤)/多阶段狂暴/闪避/减速/精英。
- **关卡 90 关 / 30 章**（全篇完结），6 种路线布局（直线/L形/S形/双路平行/汇合/交叉/三路锯齿/环路/窄道）+ 多路径分流。每关 3-5 波，首波手动开始、后续自动。
- **难度系统**（3 档）：简单/普通/困难，独立星级存档，难度顺序解锁（简单→普通→困难）。Game 构造时 `hpMul × difficultyMul`。
- **玩家档案**：本地多存档隔离（`profiles.ts`），每档案独立进度/货币/装备/天赋。
- **头衔系统**：31 阶（外门弟子→道祖），随通关章节自动晋升，晋升庆典弹窗。
- **章节缩放系统**（`hpMul`）：每关配敌人 HP 乘数（ch1=1.0→ch30=4.0）；`towerMul=sqrt(hpMul)` 自动推导塔伤害缩放，`bountyMul=sqrt(hpMul)` 经济跟上。数学保证经济-战力平衡，难度来自敌人**选型**+**机制**，hpMul 是温和微调。
- **敌人图鉴**（妖兽录）：展示全 35 种敌人属性与特性说明，辅助配塔决策。
- **寻仙抽卡**（贡献消化长效机制）：6 种奖品池（碎片/返还/天命符/仙魂×两种稀有）+ 百抽保底 + 装备碎片合成限定法宝 + 仙魂走 Modifier 管线（每5个 +1% 全体伤害，独立封顶 +30%）+ 天命符消耗品（下关 +8% 伤害）+ 抽卡返还 40 贡献
- **无尽模式**：指数 hpMul 曲线（1.05^wave）+ 敌池按波次解锁 + BOSS 每 5 波 + 贡献回流主线（min(√分,500)）+ 里程碑（wave 20/40/60/80 一次性贡献+称号）。towerMul 封顶 3.0，bountyMul 封顶 2.0，wave 70+ 自然终点。
- **难度系统**：简单/普通/困难 3 档，`hpMul × difficultyHpMul`，星级按 `${levelId}:${difficulty}` 独立存档，顺序解锁（简单→普通→困难）。
- **meta**：法宝阁/天命阶(VIP)/天赋树/皮肤/充值 5 标签"宗门"界面。
- **质量**：210 单元测试（含 challenge 11、telemetry 8、meta 经济纯函数、Modifier 多源合并、攻击策略、路径、波次、WaveManager、TowerOperations、TowerCombat 等），确定性引擎 + Modifier 管线（多来源加法合并 + 按 stat 封顶）。
- **代码分层**：`engine/`(纯逻辑) · `data/`(配置+Modifier) · `app/`(state + storyModal/levelSelect/metaScreen/profileScreen) · `repo/`(SaveRepo/IAPRepo/profiles) · `ui/`(Board) · `main.ts`(控制器)。

### v0.4 修订记录（相对 v0.3，三轮 review 的 T1-T13）

| 编号 | 修订点 | 落位置 |
|---|---|---|
| T1 | 补"跨 stat 组合规则"：同性质伤害乘子加法合并后封顶，避免跨 stat 乘法突破上限 | §9.5 |
| T2 | 5-6 魔尊战简化为静态三路（动态路径拓扑留待后续关卡） | 关卡文档 |
| T5 | 赏金公式改为基于"有效血量"（hp+shield+回血折算） | §5.1 |
| T6 | 日蚀/魅惑明确排除 AOE/aura/dot（对策成立） | §5.4 |
| T7 | 补暴击率封顶 ≤0.6、冷却缩减 ≥-40% | §9.5 |
| T9 | 撞塔击退加"同塔 6s 免疫"防连环瘫痪 | §5.4 |
| T11 | 区分小头目（isBoss=false，有bounty）与真 BOSS（贡献奖励） | §5.1 |
| T13 | 难度联动明确为"策划参照满 meta 强度手调" | §9.5 |

### v0.3 修订记录（相对 v0.2，二轮 review 的 R1-R5）

| 编号 | 修订点 | 落位置 |
|---|---|---|
| R1 | BOSS 奖励字段改名 `bossClearContribution`，明确为 meta 宗门贡献、通关结算发 | §5.1、§5.3、§6.1 |
| R2 | 高甲怪赏金上浮给出公式 `hp/10 × (1 + armor/200)` | §5.1 |
| R3 | 蛮牛撞塔规则补全：`knockback_tower`/`destroy_tower` 两种 ability + 返还/重建/对策 | §5.4 |
| R4 | BOSS 控场技能（charm/eclipse）加平衡护栏：范围受限/可视预警/时长上限/冷却免疫/对策存在 | §5.4 |
| R5 | Modifier 封顶改为按 stat 分级（伤害+150%/经济+80%/攻速射程+60%）+ 难度联动校准 | §9.5、附录A |

### v0.2 修订记录（相对 v0.1）

| 编号 | 修订点 | 落位置 |
|---|---|---|
| S1 | 补全 Modifier 运算顺序与叠加规则（add/mul_pct/mul 区分 + 上限） | §9.5 |
| S2 | 诚实界定"联网防作弊"边界，引擎改为确定性 + 固定步长 | §1.4、§12.2 |
| S3 | 修正"升级永远更划算"导致单解，改为宽度/深度张力 | §4.1、§4.3 |
| S4 | 赏金不再简单等于血量/10，BOSS 封顶、分裂子体零赏金 | §5.1、§6.1 |
| M1 | 新增 Meta 进度系统（免费玩家成长闭环） | §10 |
| M2 | 补全战斗规则：目标选择 AI、加速暂停、提前召唤、星级评定、出售规则 | §4.6、§8.6 |
| M3 | 塔新增 `attackBehavior` 字段，新机制塔也尽量零代码 | §4.4、§15 |
| M4 | 多路径关卡补全刷怪分流规则 | §8.3 |
| N1/N7 | 利息机制定档：默认关闭，启用则封顶 | §6.1、附录B |
| N3 | 新增遥测/日志系统用于平衡分析 | §14 |
| N5 | 引擎固定步长 update(fixedDt) + 渲染插值 | §1.4 |

---

## 0. 设计目标（五大可扩展性约束）

本文档所有架构决策都服务于以下五个目标，任何新功能落地前都应回头校验：

1. **剧情可扩展**：新增章节/关卡/剧情 = 加 JSON 文件，零代码。
2. **塔和敌人可扩展**：数值型新增 = 加 JSON 条目；**新行为机制**需加一个 behavior/ability 处理器（见 §4.4、§5.4）。
3. **平衡易调整**：所有数值在配置文件中，提供"平衡旋钮"，策划可独立调节。
4. **前后端可分离**：核心逻辑与数据访问通过"仓储接口（Repository）"隔离。**注**：Repo 实现可无缝替换，但**真正的服务端权威防作弊依赖确定性模拟**（见 §1.4、§12.2），需在设计期就奠定，否则联网阶段需返工。
5. **通用功能易扩展**：充值、VIP、装备、皮肤、加成等通过统一的 **Modifier（修饰器管线）** 接入，战斗结算逻辑不因新功能而改动（运算规范见 §9.5）。

---

## 1. 技术架构

### 1.1 分层总览

```
┌─────────────────────────────────────────────┐
│  表现层 UI（DOM/Canvas）                      │  ← 可替换：Web版/未来终端版
├─────────────────────────────────────────────┤
│  游戏引擎 Engine（纯逻辑，无 UI 依赖）         │  ← 核心，确定性 + 固定步长
│  · 主循环 · 战斗 · 寻路 · 波次 · 数值结算      │
├─────────────────────────────────────────────┤
│  系统层 Systems（玩家成长功能）               │  ← 装备/VIP/皮肤/存档
├─────────────────────────────────────────────┤
│  数据层 Data                                  │  ← 全部 JSON 配置
│  · Registry（按id查询） · Modifier管线        │
├─────────────────────────────────────────────┤
│  仓储层 Repository（接口）                    │  ← 前后端分离的关键
│  · SaveRepo · ProfileRepo · IAPRepo · ...    │
├──────────────────────┬──────────────────────┤
│  本地实现（localStorage） │ 远程实现（HTTP/SDK，未来）│
└──────────────────────┴──────────────────────┘
```

### 1.2 前后端分离的关键：仓储接口（Repository Pattern）

所有"数据从哪来"的问题都收敛到接口。引擎只依赖接口，不依赖具体实现。

```ts
interface SaveRepo     { load(): SaveData; save(d: SaveData): void; }       // 存档
interface ProfileRepo  { getProfile(): PlayerProfile; update(p): void; }    // 玩家档案
interface IAPRepo      { getProducts(): Product[]; purchase(id): Promise<Receipt>; } // 充值
interface LeaderboardRepo { submit(score): void; top(n): Score[]; }         // 排行榜
interface AuthRepo     { login(): Account; }                                // 账号
interface TelemetryRepo { log(event): void; }                               // 遥测（见 §14）
```

- **单机模式**：全部用 `Local*Repo`（localStorage 实现）。
- **联网模式**：换成 `Remote*Repo`（HTTP/WebSocket 实现），引擎与系统层接口调用不变。
- 切换只需在启动时注入不同实现（依赖注入）。
- ⚠️ **注意**："换实现引擎不动"指的是**数据访问层**；真正的服务端权威防作弊是另一回事，见 §12.2。

### 1.3 实际目录结构（v0.73 重构后）

```
src/
├── engine/              # 引擎（纯逻辑，确定性，可移植）
│   ├── Game.ts          # 协调查 Orchestrator（~210 行，SRP 拆分后）
│   ├── WaveManager.ts   # 波次/敌人生命周期（生成/移动/漏怪/清波）
│   ├── TowerOperations.ts # 塔操作（放置/升级/出售/索敌切换）
│   ├── WaveDirector.ts  # 波次生成 + 提前召唤
│   ├── PRNG.ts          # 种子化伪随机（确定性）
│   ├── pure/            # 纯函数子模块
│   │   ├── combat.ts    # 伤害/护甲/闪避/分裂/缩放结算
│   │   ├── economy.ts   # 塔投入/出售/升级费用计算
│   │   ├── path.ts      # 路径分段/插值/长度计算
│   │   ├── targeting.ts # 目标选择 AI（first/last/strongest/nearest）
│   │   └── waves.ts     # 波次生成（无尽模式）
│   └── combat/          # 战斗子系统
│       ├── AttackStrategies.ts # 塔行为注册表（projectile/pierce/aoe/chain/aura）
│       ├── BossAbilities.ts # BOSS 技能（enrage/charm/summon/knockback）
│       └── TowerCombat.ts # 塔战斗（攻击/弹道/伤害/光环/BOSS/撞塔）
├── data/
│   ├── ConfigLoader.ts  # 加载+校验JSON（启动时做 schema 验证）
│   ├── Registry.ts      # 按id查塔/敌人/章节
│   ├── Modifier.ts      # 修饰器管线 + 运算规范（§9.5）
│   └── config/          # 全部数值配置
│       ├── index.ts     # 集中导出
│       ├── towers.ts / enemies.ts / levels/ ...
│       ├── equipment.ts / skins.ts / vip.ts / talents.ts / titles.ts
│       ├── gacha.ts / limited_treasures.ts
│       └── story.ts
├── repo/                # 仓储接口与实现
│   ├── progress.ts      # SaveRepo 接口 + LocalSaveRepo 实现
│   ├── progressMeta.ts  # Meta 经济业务（装备/抽卡/VIP/天赋升级）
│   ├── progressLevel.ts # 关卡业务（星级/解锁/记录）
│   ├── challenge.ts     # 挑战玩法验证（纯函数）
│   ├── telemetry.ts     # 遥测 Repo 接口 + LocalTelemetryRepo
│   ├── telemetry.test.ts
│   └── progress.test.ts # 存档/Meta 测试（含挑战 14+8 tests）
├── app/                 # 应用层（UI 控制器）
│   ├── state.ts         # 全局 AppState + Modifier 构建
│   ├── metaScreen.ts    # 宗门界面（5 标签导航 + 皮肤/天命/天命阶）
│   ├── metaEquip.ts     # 法宝阁标签
│   ├── metaGacha.ts     # 寻仙抽卡标签
│   ├── metaRecharge.ts  # 充值标签
│   ├── metaShared.ts    # Meta UI 共享组件
│   ├── levelSelect.ts   # 选关/无尽模式
│   ├── storyModal.ts    # 剧情弹窗
│   └── profileScreen.ts # 玩家档案
├── ui/                  # 表现层
│   └── Board.ts         # 棋盘渲染（DOM）
├── types.ts             # 全部 TypeScript 类型定义
└── main.ts              # 入口 + Vite 挂载
```

**说明**：`telemetry/`（遥测）和 `systems/`（玩家成长业务层）尚未独立成目录——遥测通过 `TelemetryRepo` 预留接口但未实装；Meta 业务（装备/抽卡等）当前实现在 `repo/progress*.ts` + `app/meta*.ts` 中，未来联网后可抽出独立 `systems/` 层。`config/`（数值配置）实现在 `data/config/` 下，策划直接改 JSON/TS 常量文件。

### 1.4 确定性引擎与固定步长（S2 / N5）

**为什么必须现在定**：可变步长 `update(dt)` 有两个问题——① 卡顿后大步长会让敌人"瞬移"穿过塔射程而不被打到（隧穿）；② 联网防作弊需要服务端能复算战斗结果，非确定性模拟无法复算。这两个问题在 P1 写代码时就要规避，否则 P6 联网要推翻重写。

**设计约定**：
- **固定步长模拟**：逻辑步长 `fixedDt = 1/30s`，主循环用累加器（accumulator）消化实际帧时间，每攒满一个 fixedDt 推进一次逻辑；渲染层对逻辑状态做插值，保持画面流畅。
- **种子化随机**：所有随机（暴击、闪避、分裂、掉落）走 `PRNG`（如 mulberry32），种子来源于关卡种子 + 玩家操作序号。同种子 + 同操作 → 同结果。
- **关键数值整数化**：灵石、赏金、血量等经济/血量用整数运算；护甲减伤等中间量不可避免浮点，但**结算点四舍五入到整数**，保证跨平台一致。
- **禁止**在引擎层使用 `Math.random()`、`Date.now()`、浮点依赖的相等比较。

> 这样设计后，未来联网可走"客户端跑战斗 + 上报操作流水 + 服务端按种子重放校验"的真防作弊路线（§12.2），无需重写引擎。

---

## 2. 世界观与机制映射

**设定**：玩家是一脉修真宗门的护阵长老。妖族破封、魔修入侵，需在宗门各处要冲布下修士与阵法抵御妖潮。

| 塔防机制 | 仙侠化设定 |
|---|---|
| 塔 | 修士（剑修/符修/法修）或 阵法（剑阵/聚灵阵） |
| 升级 | 境界突破：炼气→筑基→金丹→元婴→化神 |
| 敌人 | 妖兽 / 魔修 / 上古凶兽 / 特殊机制怪 |
| 波次 | 妖潮的一波进攻 |
| 关卡 | 一处要冲/秘境的防守战 |
| 章节 | 一场大劫难（带剧情） |
| 兵种克制 | 五行相克 + 功法克制 |
| BOSS 波 | 上古凶兽/魔尊降临 |
| 充值货币 | 仙玉 |
| VIP | 天命阶（天命之子） |
| 装备 | 本命法宝 |
| 皮肤 | 修士外观/阵法特效 |
| 软货币 | 宗门贡献（Meta 进度，见 §10） |

---

## 3. 数据驱动的配置系统

**核心原则**：用 ID 引用，不用继承。引擎通过 `Registry` 按 id 查询配置对象，新增内容只加 JSON。

```ts
class Registry {
  tower(id): TowerConfig;       // 查塔
  enemy(id): EnemyConfig;       // 查敌人
  chapter(id): ChapterConfig;   // 查章节
  equipment(id), skin(id), vip(level) ...
}
```

### 3.1 配置校验

`ConfigLoader` 加载时做 schema 校验（必填字段、数值范围、id 唯一性、引用完整性、behavior/ability 名是否已注册），配置错误在启动时报错而非运行时崩溃。

### 3.2 扩展性的两类区分（重要）

| 扩展类型 | 是否零代码 | 例子 |
|---|---|---|
| **数值/组合扩展** | ✅ 零代码（加 JSON 条目） | 新增一个"火鸦符"塔，复用 `projectile` behavior |
| **新行为机制扩展** | ❌ 需加处理器 | 新增"召唤类塔"需注册新 `summon` behavior 处理器 |

文档中所有"零代码"承诺均指第一类。第二类已在引擎预留 behavior/ability 注册表（§4.4、§5.4），新增处理器是局部改动，不影响其它内容。

---

## 4. 塔系统（含完整数值）

### 4.1 设计基准（S3 修订）

- **基准效率**：`1 灵石 ≈ 5 单体 DPS`（单体均衡塔的标准）。
- **功能性塔**（减速/经济/AOE）效率略低，价值在机制。
- ⚠️ **宽度 vs 深度张力**（修订）：升级效率**不应严格单调递增**。升级一座塔提升 DPS，但**射程覆盖有限**，面对多路线/分散刷怪时单塔守不住；铺多座低级塔覆盖广但单点 DPS 不足、难破厚血怪。两者必须各有适用场景，避免"养一座化神塔就赢"的单解。
- 所有数值在 `config/towers.json`，可调。

### 4.2 12 种塔数值表

| 塔 | 流派 | 建造费 | 炼气DPS | 射程(格) | 攻速/s | attackBehavior | 定位 |
|---|---|---|---|---|---|---|---|
| 飞剑修士 | 剑 | 80 | 20 | 2.5 | 1.0 | projectile | 单体均衡（基准） |
| 重剑修士 | 剑 | 120 | 38 | 2.0 | 0.5 | hitscan | 单发高伤破甲，克BOSS |
| 剑阵 | 剑 | 200 | 30(AOE) | 2.2 | 2.0 | aoe | 范围持续割伤 |
| 符箓修士 | 符(弓) | 70 | 18 | 3.5 | 1.5 | projectile | 远程速射，射程最远 |
| 冰符修士 | 符 | 100 | 8 | 2.8 | 1.0 | projectile+slow | 减速50%+冰冻，控场 |
| 毒符修士 | 符 | 110 | 12(毒) | 2.8 | 1.0 | dot | 叠毒持续伤，克厚血 |
| 长枪兵修 | 枪 | 75 | 22 | 2.5 | 1.0 | pierce | 直线穿透，克密集 |
| 火法修士 | 法 | 130 | 28(AOE) | 2.5 | 0.8 | aoe | 范围溅射，克虫群 |
| 雷法修士 | 法 | 150 | 30(链) | 2.8 | 0.7 | chain(3) | 链电跳3目标 |
| 聚灵阵 | 阵 | 90 | 0 | 2.5光环 | — | aura | 周围塔+25%攻速伤害 |
| 禁制阵 | 阵 | 110 | 6 | 2.5 | 1.0 | aoe+debuff | 减速+破隐+扣血上限 |
| 回灵丹炉 | 阵 | 100 | 0 | — | — | economy | 经济塔，每波产灵石 |

### 4.3 境界突破数值与效率曲线（S3 修订）

修订点：效率曲线**不再严格递增**，而是"轻微上升 + 强烈覆盖约束"，让铺塔流保留竞争力。

| 境界 | 升级费 | 累计投入 | DPS | 边际效率(ΔDPS/Δ灵石) | 新机制 |
|---|---|---|---|---|---|
| 炼气(初始) | 建造80 | 80 | 20 | 0.25 | — |
| 筑基 | +60 | 140 | 40 | 0.33 | — |
| 金丹 | +100 | 240 | 80 | 0.40 | 暴击+20% |
| 元婴 | +170 | 410 | 150 | 0.41 | 剑气穿透 |
| 化神 | +280 | 690 | 260 | 0.39 | 万剑归宗：普攻分裂3道 |

> 边际效率在元婴达峰（0.41）后回落（化神 0.39），意味着**盲目升满不如分散投资**。再叠加"射程覆盖不变、多路线顾此失彼"，铺塔与升级两条路线各有最优场景。

### 4.4 塔配置示例与 behavior 字段（M3）

```jsonc
{
  "flying_sword": {
    "name": "飞剑修士", "school": "sword", "icon": "⚔️",
    "attackBehavior": "projectile",        // 攻击行为类型，引擎按此派发处理器
    "targetPolicy": "first",               // 默认目标选择（见 §4.6），玩家可切换
    "cost": 80, "sellRatio": 0.6,
    "levels": [
      { "realm": "炼气", "dmg": 20, "rate": 1.0, "range": 2.5 },
      { "realm": "筑基", "dmg": 40, "rate": 1.2, "range": 2.7, "upgradeCost": 60 },
      { "realm": "金丹", "dmg": 80, "rate": 1.5, "range": 3.0, "upgradeCost": 100, "crit": 0.2 },
      { "realm": "元婴", "dmg": 150, "rate": 1.8, "range": 3.3, "upgradeCost": 170, "pierce": true },
      { "realm": "化神", "dmg": 260, "rate": 2.2, "range": 3.6, "upgradeCost": 280, "split": 3 }
    ]
  }
}
```

**已注册 behavior**（引擎实现这些处理器，配置引用即可）：

| behavior | 说明 |
|---|---|
| `projectile` | 飞行物命中单体 |
| `hitscan` | 即时命中单体 |
| `pierce` | 直线穿透多目标 |
| `aoe` | 范围溅射 |
| `chain:N` | 链式跳跃 N 目标 |
| `dot` | 持续伤害（毒/火池） |
| `aura` | 范围光环 buff 友军 |
| `economy` | 经济产出 |

新增 behavior = 在 CombatSystem 注册一个处理器 + 配置引用，其它塔不受影响。

### 4.5 五行 / 功法克制（可选深度，可后置）

- 火法克"木灵虫群"、冰符控"蛮兽冲锋"、重剑打"高护甲魔甲"、雷法克"水属性妖修"。
- 实现：敌人 `element/tags`，塔 `bonusVs`，结算乘克制系数（默认 1.5×）。
- Phase 1 可先关闭，用 `enabled: false`。

### 4.6 战斗规则（M2 新增）

塔防标配机制，引擎与配置均已预留：

**① 目标选择 AI（Targeting）**
每座塔有 `targetPolicy`，玩家可在战斗中切换：
- `first`（最前/最接近出口）、`last`（最后/刚出生）
- `strongest`（最高血量）、`weakest`（最低血量）、`nearest`（最近）

**② 加速与暂停**
1× / 2× / 3× / 暂停。实现上 = 调整固定步长推进频率（§1.4），暂停冻结逻辑帧。加速不破坏确定性（只是单位时间内推进更多步）。

**③ 提前召唤下一波**
玩家可在当前波未清完时手动召唤下一波，获得**提前召唤奖励**（如 +20% 该波赏金）。既是节奏控制，也是经济杠杆（高手用它滚雪球）。

**④ 出售规则**
- 波次间隙：按 `sellRatio`（默认 0.6）返还。
- 波次进行中：允许出售但**返还折半**（0.3），避免频繁拆建刷经济。

**⑤ 三星评定标准**（§8.6 详述，此处先给定规则）
基于剩余血量评定（全关统一 3 心）：3 心（零漏怪）3 星、剩余 ≥2 心（漏 ≤1）两星、通关即一星。星级解锁 meta 奖励（§10）。

---

## 5. 敌人系统（含完整数值）

### 5.1 单位约定（S4 修订）

- **速度**：格/秒。典型路径长 16 格，1.0 速度 = 16 秒走完全程。
- **护甲**：`实际伤害 = 攻击力 × (100/(100+护甲))`。护甲 40 → 只受 71% 伤害。
- ⚠️ **赏金独立配置**（修订）：赏金**不再强制 = 血量/10**，改为 `bounty` 字段独立配置，遵循四条规则：
  1. **普通怪** bounty ≈ 血量/10（保持基准）；
  2. **高护甲/护盾/回血怪** bounty 按"有效血量"上浮，补偿玩家更高投入：
     `bounty = round( 有效血量/10 × (1 + armor/200) )`，**有效血量 = hp + shield + 回血折算**（傀儡 400hp/40甲 → 48≈50；魔修喽啰 250hp+50盾 → 30；血修按回血折算上调）。公式仅作参考，策划可直接手填覆盖；
  3. **BOSS** bounty = 0（击杀不给关卡内灵石），改为通关结算时发放 **`bossClearContribution`（宗门贡献，归 meta，见 §10）**，彻底不进关卡内即时经济循环，防止中段 BOSS 打爆后续平衡；
  4. **分裂子体** bounty = 0 或大幅降低（防分身妖刷钱）。

**小头目 vs BOSS 的区分（T11）**：
- **小头目**（山猪王、魔修统领等）：`isBoss: false`，是"血量偏高的普通敌人"，有 bounty、走正常波次经济、**不给** `bossClearContribution`；
- **真 BOSS**（九尾天狐、烛龙、魔尊血煞）：`isBoss: true`，bounty=0、通关发 `bossClearContribution`、带 BOSS 技能（受 §5.4 护栏约束）。

### 5.2 15 种敌人数值表

| 敌人 | 血量 | 速度 | 护甲 | 赏金 | 特性 |
|---|---|---|---|---|---|
| 妖狼 | 60 | 1.2 | 0 | 6 | 成群 |
| 山猪妖 | 180 | 0.7 | 8 | 18 | 厚皮减伤 |
| 蛇妖 | 90 | 1.0 | 2 | 9 | 25%闪避远程 |
| 蝙蝠群 | 30 | 1.8 | 0 | 3 | 飞行，免疫近战 |
| 蛮牛巨兽 | 600 | 0.6 | 15 | 55 | 撞塔，漏过=毁塔 |
| 毒沼蟾 | 200 | 0.8 | 4 | 20 | 死留毒沼 |
| 隐身狐妖 | 150 | 1.1 | 0 | 15 | 隐身(需破隐) |
| 分身妖 | 120 | 1.0 | 0 | 12 | 死亡分裂×2（子体赏金0） |
| 魔修喽啰 | 250 | 0.9 | 0+50盾 | 25 | 护体魔气(先破盾) |
| 血修 | 220 | 0.9 | 5 | 22 | 受击回血，需爆发 |
| 魔甲傀儡 | 400 | 0.6 | 40 | 50 | 极高甲，物理刮痧（赏金上浮） |
| 御剑魔修 | 180 | 1.3 | 3 | 18 | 移动反伤周围塔 |
| 九尾天狐(BOSS) | 3000 | 0.7 | 20 | 0(通关奖) | 魅惑+召唤 |
| 烛龙(BOSS) | 5000 | 0.5 | 30 | 0(通关奖) | 周期日蚀致盲 |
| 魔尊血煞(终BOSS) | 8000 | 0.6 | 40 | 0(通关奖) | 多阶段 |

> BOSS 赏金改为"通关固定奖励"（见 §6.1），击杀 BOSS 本身不给关卡内灵石，避免经济爆裂。

### 5.3 敌人配置示例

```jsonc
{
  "wolf": { "name": "妖狼", "icon": "🐺", "hp": 60, "speed": 1.2, "armor": 0, "bounty": 6, "tags": ["beast"] },
  "splitter": { "name": "分身妖", "hp": 120, "speed": 1.0, "armor": 0, "bounty": 12,
                "abilities": ["split:2"], "splitChildBounty": 0 },
  "magic_puppet": { "name": "魔甲傀儡", "hp": 400, "speed": 0.6, "armor": 40, "bounty": 50, "tags": ["mechanical","highArmor"] },
  "fox_boss": { "name": "九尾天狐", "hp": 3000, "speed": 0.7, "armor": 20, "bounty": 0, "isBoss": true,
                "bossClearContribution": 300, "abilities": ["charm", "summon:fox"] }
}
```

### 5.4 特殊机制实现预留

每种机制怪的行为通过 `abilities` 字段声明，引擎按能力名查找处理器（策略模式）。新增机制 = 加一个 ability 处理器 + 配置引用（第二类扩展，见 §3.2）。

| ability | 行为 |
|---|---|
| `shield:N` | 护盾层，先破盾 |
| `lifesteal:ratio` | 受击回血 |
| `split:N` | 死亡分裂（子体赏金单独配置） |
| `stealth` | 隐身，需破隐塔 |
| `fly` | 飞行，免疫近战 |
| `explode` | 死亡爆炸 |
| `reflect` | 移动反伤 |
| `knockback_tower` | 经过塔时**击退**塔（塔短暂瘫痪 2s，不销毁，可恢复） |
| `destroy_tower` | 撞到塔时**摧毁**塔（玩家损失该塔，按 `sellRatio` 返还灵石，可在原位或别处重建） |
| `charm` | BOSS 魅惑（见下方护栏） |
| `eclipse` | BOSS 日蚀致盲（见下方护栏） |

**控场技能平衡护栏（R4，防软锁）**：
所有"使塔失去作用"的技能必须满足以下约束，避免玩家被无解恶心：
- **范围受限**：只影响技能作用圈内的塔（如祭坛范围/全屏需配置明示），不存在"凭空影响全场且无对策"；
- **可视预警**：触发前 2-3s 给明显视觉预警（地面红圈/天色变暗），玩家可提前应变（提前召唤、调整目标策略）；
- **持续时间上限**：单次 ≤ 3s；
- **冷却免疫**：同一塔被作用后有 6-8s 免疫，防连续软锁；
- **对策存在**：致盲（日蚀）/魅惑只削弱**需要命中判定**的攻击（`projectile`/`hitscan`/`pierce`/`chain`）；**AOE 区域伤害、`aura` 光环、`dot` 持续伤害**不依赖命中判定，**不受日蚀/魅惑影响**，给玩家留明确反制手段（T6）。

**撞塔类敌人规则（R3）**：
- 蛮牛默认用 `knockback_tower`（击退，温和）；高难模式或精英变种可配 `destroy_tower`（摧毁，严厉）；
- 被摧毁的塔按 `sellRatio` 返还灵石，玩家可重建；
- 玩家对策：用减速/冰冻塔先控住蛮牛，或把核心塔放在路径拐角后方（蛮牛直线冲撞时不易触及）；
- **连环击退护栏（T9）**：同一塔被击退后有 **6s 免疫**，防止一群蛮牛连续通过导致某塔被永久瘫痪。

---

## 6. 经济 / 资源系统

### 6.1 灵石来源（S4 / N1 / N7 修订）

| 来源 | 数值 | 说明 |
|---|---|---|
| 开局发放 | `startStones`（关卡配置，如 300） | 每关重置 |
| 击杀赏金 | 敌人 `bounty` 字段（独立配置，见 §5.1） | 主收入 |
| 波次清波奖 | `clearBonus`（50~120递增） | 鼓励守完 |
| 提前召唤奖励 | 下一波赏金 × `earlyCallBonus`（默认 +20%） | 高手杠杆（§4.6③） |
| BOSS 通关奖 | `bossClearContribution`（宗门贡献，通关结算发，归 meta §10） | 不进关卡内灵石循环 |
| 回灵丹炉塔 | 每波末 8（炼气）→20（化神） | 经济塔 |
| 利息（默认关闭） | 见下 | N1/N7 |

**利息机制定档（N1/N7）**：默认**关闭**。若某关卡设计需要，可逐关开启并**强制封顶**：
- 公式：每波末 `+ min(当前灵石 × 3%, 利息上限)`；
- 利息上限默认 20 灵石/波（可关卡级配置）；
- 设计意图：防止"苟着吃利息"成为通解，封顶后利息只是小额补贴而非核心策略。

### 6.2 灵石去向

| 去向 | 说明 |
|---|---|
| 建塔 | 一次性建造费 |
| 突破境界 | 每境界递增 |
| 出售 | 间隙 0.6、波次中 0.3（§4.6④） |

### 6.3 漏怪惩罚（3 心制）

每漏 1 只扣 1 心，全关统一 3 心（`lives: 3`）。漏怪 = 没赏金 = 经济落后，形成滚雪球压力但不直接破产。BOSS/精英/普通漏怪均 1 心，BOSS 的威胁来自其战斗能力而非额外扣血。

---

## 7. 平衡性系统

### 7.1 难度由三层叠加（每层独立可调）

**① 关卡内波次递增**
```
第N波敌人血量 = 基础血量 × (1 + globalHpGrowth × (N-1))   // globalHpGrowth 默认 0.15
第N波数量     = 基础数量 + ceil(N/2)
```

**② 章节间基础值递增**

| 章 | 妖狼基础血量 | 引入新怪 | 建议塔均境 |
|---|---|---|---|
| 1 | 60 | 妖狼/山猪 | 炼气~筑基 |
| 2 | 110 | 蛮牛/毒蟾/蝙蝠 | 筑基~金丹 |
| 3 | 180 | 魔修/血修/傀儡 | 金丹~元婴 |
| 4 | 280 | 隐身狐/分身/BOSS | 元婴 |
| 5 | 400 | 终极BOSS综合 | 元婴~化神 |

**③ 全局难度倍率**（玩家自选）

| 难度 | 血量倍率 | 赏金倍率 |
|---|---|---|
| 炼气(简单) | ×0.8 | ×1.1 |
| 筑基(普通) | ×1.0 | ×1.0 |
| 金丹(困难) | ×1.4 | ×0.9 |
| 元婴(噩梦) | ×1.9 | ×0.8 |

### 7.2 平衡演算

**场景**：第1章第1关第1波，8只妖狼，开局300灵石。
- 建 3 飞剑(240) + 1 符箓(60) = 4 塔，总 DPS = 78。
- 妖狼血60，速度1.2，射程2.5覆盖约4格路径 → 在射程内 `4/1.2 ≈ 3.3秒`。
- 单飞剑 3.3 秒输出 = 66 > 60 ✓ 路径内可杀一只。
- 总 DPS 78 × 3.3s = 257，足够杀 4 只；射程重叠后首波轻松守住 ✓。
- 进账：8×6 赏金 + 50 清波 = 98 灵石。

**反例（难度梯度）**：第3关魔修喽啰 250血+50盾。炼气飞剑 DPS20 需 15 秒，但路径覆盖仅 3.3 秒 → **必须升级到金丹或换雷法/重剑**，逼玩家成长 ✓。

**宽度 vs 深度张力验证**（S3）：化神飞剑 DPS 260、射程 3.6 格，单塔只能覆盖一段路径。面对第4章**双路线 + 分散刷怪**，一座化神塔守不住两路，玩家必须**分兵铺塔**或用聚灵阵增益多塔——证明铺塔流有存在价值 ✓。

### 7.3 平衡"旋钮"一览

| 旋钮 | 作用 | 位置 |
|---|---|---|
| `startStones` | 开局灵石松紧 | 关卡JSON |
| `globalHpGrowth` | 波次血量增长 | 章节JSON |
| 难度倍率 | 整体难度 | manifest |
| 塔 DPS/费/射程/behavior | 塔强弱 | towers.json |
| 敌人血/速/甲/bounty | 怪强弱 | enemies.json |
| `clearBonus`/赏金系数 | 经济松紧 | 波次/敌人 |
| 升级边际效率曲线 | 铺塔 vs 升级平衡 | towers.json levels |

> 平衡核心公式：**塔 DPS × 路径覆盖时间 ≥ 敌人血量** 才能守住；**升级 DPS 收益 vs 多塔覆盖收益**决定战术形态。

---

## 8. 章节 / 关卡 / 剧情系统（可扩展核心）

### 8.1 数据驱动的章节

```
config/chapters/
├── _manifest.json        ← 章节顺序、解锁条件、难度倍率
├── ch1_山门初劫.json
├── ch2_万妖攻山.json
└── ch3_魔修乱世.json      ← 加新章节：丢文件 + manifest 加一行
```

**引擎只认 manifest + 文件，不写死任何章节数/关卡数/波次数**。未来 DLC、活动关卡、限时挑战都是加文件。

### 8.2 manifest.json

```jsonc
{
  "chapters": [
    { "id": "ch1", "file": "ch1_山门初劫.json", "unlock": null },
    { "id": "ch2", "file": "ch2_万妖攻山.json", "unlock": { "clear": "ch1" } },
    { "id": "ch3", "file": "ch3_魔修乱世.json", "unlock": { "clear": "ch2", "stars": 6 } }
    // 加第6章在此追加一行
  ],
  "difficultyMultipliers": { "炼气": 0.8, "筑基": 1.0, "金丹": 1.4, "元婴": 1.9 }
}
```

### 8.3 单个章节文件结构（M4 修订：多路径分流）

```jsonc
{
  "id": "ch1", "title": "山门初劫",
  "story": {
    "intro": "开场剧情文本……",
    "between": ["关卡1-2间过场", "关卡2-3间过场"],
    "outro": "通关结算剧情……"
  },
  "globalHpGrowth": 0.15,
  "levels": [
    {
      "id": "ch1-l1", "name": "前山门",
      "startStones": 300, "lives": 3,
      "grid": { "w": 16, "h": 10 },
      "paths": [
        { "id": "north", "waypoints": [[0,2],[15,2]] },
        { "id": "south", "waypoints": [[0,8],[15,8]] }
      ],
      "buildableCells": [ /* 可建塔格子 */ ],
      "waves": [
        { "spawns": [
            {"enemy":"wolf","count":8,"gap":0.8,"delay":0,"path":"north"},
            {"enemy":"wolf","count":4,"gap":1.0,"delay":2,"path":"south"}
          ],
          "clearBonus": 50
        }
      ]
    }
  ]
}
```

**多路径分流规则（M4）**：
- spawn 的 `path` 字段指定该批怪走哪条路（`"all"` 表示均分）；
- 缺省 `path: "all"`：按路径数均分；
- UI 在波次开始前**预告刷怪路线与种类**（小图标在路径起点），玩家据此布阵；
- 不同路径漏怪都扣同一池 3 心。

### 8.4 剧情可内容设计

- 剧情为**纯文本结构**（intro/between/outro），存于章节 JSON，渲染层（StoryPanel）负责表现。
- 未来扩展：分支剧情、选项、配音——只需扩展 story 结构 + 渲染器，引擎不动。
- 支持活动/限时章节：manifest 加 `availability: { type: "event", until: "2026-12-31" }`。

### 8.5 章节编排示例（5章）

| 章 | 标题 | 剧情 | 关卡数 | 新机制引入 |
|---|---|---|---|---|
| 1 | 山门初劫 | 妖狼群袭山门，新晋弟子守阵 | 3 | 基础塔、单一路径 |
| 2 | 万妖攻山 | 蛮牛、毒蟾围攻，多路径 | 4 | 多路线、经济塔 |
| 3 | 魔修乱世 | 魔修破阵入侵，护盾怪 | 5 | 护盾、真实伤害 |
| 4 | 秘境凶兽 | 反攻妖兽秘境，BOSS战 | 5 | BOSS机制、禁制阵 |
| 5 | 血煞魔尊 | 终极决战，多阶段BOSS | 6 | 全机制综合 |

### 8.6 星级评定（M2，3 心制）

全关统一 `lives: 3`。通关后按剩余心数评定：

| 星级 | 条件 | 作用 |
|---|---|---|
| ⭐⭐⭐ | 3 心（零漏怪） | 解锁 meta 稀有掉落、章节解锁条件 |
| ⭐⭐ | ≥2 心（漏 ≤1） | 普通 meta 奖励 |
| ⭐ | 通关即可 | 基础奖励、推进进度 |

> manifest 的 `unlock.stars` 指该章累计星数门槛。星级阈值可在 manifest 全局调。

---

## 9. 玩家成长与加成系统（Modifier 管线）

### 9.1 统一加成管线（可扩展性的核心）

所有"加成"——装备、皮肤、VIP、科技树、活动 buff——都通过同一条 Modifier 管线注入战斗结算，**结算逻辑不因新功能而改动**。运算规范见 §9.5。

```ts
// 任意一次伤害/金币结算 = 基础值 经过 Modifier 管线（按 §9.5 公式结算）
function computeDamage(base, ctx) {
  return Modifier.resolve(base, ctx, [
    equipmentMod(player.equipment),
    skinMod(player.skin),
    vipMod(player.vip),
    techMod(player.tech),
    eventBuffMod(player.buffs),
  ]);
}
```

### 9.2 装备 / 本命法宝（equipment.json）

```jsonc
{
  "green_lotus_sword": { "name": "青莲剑", "slot": "weapon", "rarity": "epic",
    "mods": [{ "stat": "swordDmg", "op": "mul_pct", "value": 0.15 }] },
  "lifire_fan": { "name": "离火扇", "slot": "weapon", "rarity": "legend",
    "mods": [{ "stat": "fireRange", "op": "add", "value": 0.5 }] }
}
```

- 槽位：weapon / armor / accessory（可扩展）。
- 获取：关卡掉落、商店购买、抽卡（"寻仙"），主要走 Meta 进度（§10）。
- 玩家档案存储已拥有装备 + 装备槽配置。

### 9.3 皮肤（skins.json）

```jsonc
{
  "flying_sword_immortal": {
    "target": "flying_sword",
    "name": "仙人临凡皮肤", "rarity": "legend",
    "visual": { "icon": "🗡️", "effect": "golden_aura" },
    "mods": [{ "stat": "dmg", "op": "mul_pct", "value": 0.05 }]
  }
}
```

- 皮肤主要改外观，部分带微量属性（付费皮肤卖点）。
- 复用同一 Modifier 管线。

### 9.4 天命阶 VIP（vip.json）

```jsonc
{
  "levels": [
    { "level": 0, "name": "凡人",  "mods": [] },
    { "level": 1, "name": "天命一阶", "price": 30,
      "mods": [{"stat":"bountyMul","op":"mul_pct","value":0.10}] },
    { "level": 2, "name": "天命二阶", "price": 98,
      "mods": [{"stat":"bountyMul","op":"mul_pct","value":0.15},{"stat":"startStonesAdd","op":"add","value":50}] }
  ]
}
```

- VIP 加成走 Modifier 管线，不影响战斗代码。
- VIP 等级存储于玩家档案，由充值驱动（§11）。

### 9.5 Modifier 运算规范（S1 新增——必读）

为避免 `add`/`mul` 混用导致顺序敏感、数值爆炸，统一定义三类操作与**固定结算公式**（结算顺序由公式决定，与 mod 在数组中的位置无关）：

**三类操作**：

| op | 含义 | 叠加方式 | 用途 |
|---|---|---|---|
| `add` | 扁平加成 | 所有 `add` 求和 | 离火扇 +0.5 射程、VIP +50 开局灵石 |
| `mul_pct` | 百分比加成 | **加法叠加**（Σ value） | 绝大多数加成（装备/皮肤/VIP 伤害/赏金%） |
| `mul` | 真实乘法 | **乘法叠加**（Π value） | 仅特殊场景，慎用（默认不开放给配置） |

**统一结算公式**：

```
final = (base + Σadd) × (1 + Σmul_pct) × Πmul
```

- 同一 `stat` 的所有 `add` 先求和，所有 `mul_pct` 加法求和，所有 `mul` 乘法求积；
- 顺序固定为 `(base+add) → mul_pct → mul`，**与 mod 注册顺序无关**。

**举例**：base=100，装备 swordDmg `mul_pct 0.15`，VIP swordDmg `mul_pct 0.10`，科技 swordDmg `mul_pct 0.20`：
```
final = 100 × (1 + 0.15+0.10+0.20) = 100 × 1.45 = 145   // 加法叠加，可控
```
（若误用乘法叠加：1.15×1.10×1.20 = 1.518，难以预测且易膨胀。）

**跨 stat 组合规则（T1，关键）**：
§9.5 只定义了**同一 stat 内**加法叠加。但一座塔常同时受多个同性质 stat 影响（如飞剑修士同时吃 `swordDmg` 流派乘子和 `dmg` 通用乘子）。为避免跨 stat 乘法突破封顶、保证数学可控，规定：
- 结算时按目标**适用的 stat 集合**，把所有"伤害类"乘子（`dmg`/`swordDmg`/`bowDmg`/`spearDmg`/`magicDmg`/`critDmg` 折算等）**先合并为一个有效伤害乘子**：`effectiveDmgMul = Σ(所有适用伤害类 stat 的 Σmul_pct)`；
- 即流派乘子与通用乘子**加法合并**，而非相乘；
- 合并后的 `effectiveDmgMul` 统一受"伤害类 +150%"封顶约束；
- 非伤害类（攻速/射程/经济/减速）各自独立按 stat 封顶。

举例（飞剑修士）：`swordDmg Σ=0.35`、`dmg Σ=0.27` → 不是 `1.35×1.27`，而是 `effectiveDmgMul = 0.62`，`final = base × 1.62`，受 +150% 封顶保护。

**上限（防膨胀，按 stat 分级，R5）**：
为避免后期 meta 天赋+装备+VIP 叠加把难度打穿，封顶**按 stat 分级**（而非统一 +200%）：

| stat 类别 | `Σmul_pct` 封顶 | 说明 |
|---|---|---|
| 伤害类（dmg/swordDmg/...，合并后） | +150% | 战斗力天花板（T1 合并后封顶） |
| 经济类（bountyMul/...） | +80% | 防经济爆裂 |
| 攻速/射程类 | +60% | 防机制失衡 |
| `add` 类 | ≤ 基础值的 1 倍 | 防极端溢出 |
| 暴击率（crit） | ≤ 0.6（绝对值） | 防必暴（T7） |
| 冷却缩减（cooldown） | ≥ -40% | 防技能无CD（T7） |

- 超过上限的部分截断，并写遥测告警（§14）便于发现平衡异常；
- **难度联动校准（T13）**：高难度（金丹/元婴）的敌人倍率**由策划参照"满 meta 玩家强度"手动配置**（非系统自动），确保付费/长线玩家在噩梦难度仍有挑战。

**实现**：`Modifier.resolve(base, ctx, sources)` 内部按 stat 分组、分类求和、套公式、套上限，返回最终值。新增加成来源只需产出 mod 数组，无需改 resolve。

---

## 10. Meta 进度系统（M1 新增——免费玩家成长闭环）

灵石不跨关、仙玉是付费货币，**免费玩家必须有独立的成长闭环**，否则通关即流失。本系统是 F2P 留存核心，非可选。

### 10.1 软货币：宗门贡献

| 货币 | 获取 | 用途 |
|---|---|---|
| 宗门贡献（软货币） | 通关、日常、三星 | 抽装备/皮肤（寻仙）、升级 meta 天赋 |

### 10.2 获取来源

| 来源 | 量级 | 频次 |
|---|---|---|
| 通关奖励 | 每关 20~50 | 每关首通更高 |
| 三星奖励 | 额外 ×1.5 | 鼓励冲星 |
| 日常任务 | 如"用剑修通关1关" | 每日刷新 |
| 签到 | 累登奖励 | 每日 |
| 困难度加成 | 困难/噩梦额外产出 | 鼓励挑战 |

### 10.3 消耗（寻仙抽卡）

- 单抽 / 十连（十连保底稀有）；
- 池子：装备池、皮肤池、混合池；
- 保底机制（软保底 + 硬保底）配置化；
- 重复获得转为"分解贡献"（仙侠包装：化灵回收）。

### 10.4 Meta 天赋（可选深度）

跨关卡永久加成（如"全体剑修 +5% 伤害"），用贡献升级，走 Modifier 管线。这是免费玩家追平付费玩家的长线养成线。

### 10.5 留存节奏

日常任务 + 签到 + 首通/冲星奖励构成"每日有事做"的循环；新章节/DLC 提供长期目标。该系统通过 `Meta` 模块 + `ProfileRepo` 实现，联网后服务端校验贡献收支防刷。

---

## 11. 充值 / 商业化系统

### 11.1 货币体系

| 货币 | 用途 | 来源 |
|---|---|---|
| 灵石 | 关卡内建塔/升级 | 关卡内获取，不跨关 |
| 仙玉 | 充值货币，买皮肤/VIP/抽卡/体力 | 充值（IAPRepo） |
| 宗门贡献 | 免费养成（§10） | 通关/日常/三星 |

### 11.2 充值流程（前后端分离）

```
玩家点充值 → UI 调 IAPRepo.purchase(productId)
   ↓
单机：LocalIAPRepo 直接发仙玉（mock）
联网：RemoteIAPRepo → 后端创建订单 → 支付SDK → 回调校验 → 发货
```

- 引擎只调 `IAPRepo`，不关心实现。
- 联网时所有仙玉/道具变更**必须服务端校验**（防作弊，见 §12.2）。

### 11.3 商品配置（iap.json）

```jsonc
{
  "products": [
    { "id": "jade_60",   "price": 6,   "currency": "CNY", "reward": {"jade": 60} },
    { "id": "jade_328",  "price": 30,  "currency": "CNY", "reward": {"jade": 328} },
    { "id": "monthly",   "price": 30,  "reward": {"type":"monthly","jadePerDay":10} }
  ]
}
```

### 11.4 商业化点（仙侠包装，不违和）

| 功能 | 包装 |
|---|---|
| 充值货币 | 仙玉 |
| VIP | 天命阶（天命之子，气运加成） |
| 月卡 | 宗门供奉 |
| 装备 | 本命法宝 |
| 皮肤 | 修士外观/阵法特效 |
| 抽卡 | 寻仙（仙玉抽付费池，贡献抽免费池） |

---

## 12. 联网 / 账号系统

### 12.1 渐进式联网策略

| 阶段 | 形态 | 实现 | 防作弊强度 |
|---|---|---|---|
| A | 纯单机 | 全部 Local*Repo | 无需 |
| B | 云存档 | SaveRepo 换 Remote + 本地缓存 | 弱（存档同步） |
| C | 账号 + 充值 | AuthRepo + IAPRepo 换 Remote | 中（敏感数据服务端权威） |
| D | 排行榜/社交 | LeaderboardRepo + 好友/公会 | 中 |
| E（可选） | 服务端权威战斗 | 操作流水重放校验 | 强 |

切换 = 启动时注入不同 Repo 实现，**数据访问层零改动**。

### 12.2 防作弊边界（S2 修订——诚实界定）

**关键认知**：客户端永远不可信。不同阶段能做到的防作弊强度不同，文档不再承诺"配置校验即可防刷"：

- **C 阶段（账号+充值）**：仙玉/道具/VIP 等**敏感数据服务端为唯一真源**，客户端只读。这一层是扎实的，能挡住直接改存档刷仙玉。
- **战斗结果**：客户端跑战斗、上报"通关/星级"。服务端能做的**轻度校验**：通关时长下限、星级与血量一致性、关卡解锁前置。**这挡不住"假装正常通关骗奖励"**——因为战斗逻辑在客户端，服务端无法仅凭结果判断真伪。
- **E 阶段（可选，强防作弊）**：服务端用**确定性模拟**（§1.4）重放客户端上报的操作流水，校验最终状态是否一致，不一致即判作弊。**正因为引擎从 P1 就是确定性的**（固定步长 + 种子 PRNG + 整数结算），这条路才可行，无需重写引擎。

> 结论：P1~P5 单机/mock 阶段不操心；P6 上线先做到 C 阶段（敏感数据服务端权威 + 战斗轻度校验）；若日后作弊严重，再上 E 阶段重放校验。**确定性引擎是这条演进路线的入场券，现在就要埋好。**

### 12.3 玩家档案结构（PlayerProfile）

```ts
interface PlayerProfile {
  accountId?: string;
  jade: number;                  // 仙玉（联网后服务端权威）
  contribution: number;          // 宗门贡献（§10）
  vipLevel: number;
  equipment: { owned: string[]; slots: Record<Slot, string|null> };
  skins: { owned: string[]; equipped: Record<string, string> };
  meta: { dailyTasks: ...; signIn: ...; };         // Meta 进度
  tech: Record<string, number>;
  progress: {
    chapters: Record<chapterId, { unlocked: boolean; levels: Record<levelId, {stars, bestScore}> }>;
  };
  settings: { difficulty, sound, ... };
}
```

---

## 13. 存档系统

- **关卡内进度**：可中断续战（保存当前波次/塔布局/灵石 + 关卡种子，便于确定性续算）。
- **玩家档案**：跨关卡的养成（VIP/装备/皮肤/通关进度/Meta）。
- 通过 `SaveRepo` 抽象，本地用 localStorage，联网后换云端。
- 存档做版本号（`schemaVersion`），便于未来迁移。
- **云存档冲突策略**（联网后）：以"最后修改时间"为准 + 保留两端历史副本供玩家选择；关键经济字段（仙玉/贡献）以服务端为准，不本地合并。
- localStorage 是同步阻塞且有 ~5-10MB 上限：存档写入做 **debounce + 异步封装**，避免卡帧。

---

## 14. 遥测与日志系统（N3 新增）

平衡调参不能只靠"策划手感"，需要数据支撑。引擎预留战斗事件日志。

### 14.1 记录的事件

| 事件 | 字段 | 用途 |
|---|---|---|
| 敌人死亡 | enemyId, 死因(哪座塔/buff), 受到的总伤害 | 分析实际 DPS、克制是否生效 |
| 塔输出统计 | towerId, behavior, 累计伤害, 攻击次数 | 算真实 DPS/灵石效率 |
| 漏怪 | enemyId, 路径, 当时塔布局 | 找防守盲区 |
| 经济流水 | 灵石来源/去向逐笔 | 验证经济曲线 |
| Modifier 触发 | stat, 各来源贡献, 是否触顶 | 发现加成异常（§9.5） |
| 通关/失败 | 关卡, 难度, 用时, 星级, 操作流水 | 平衡 + 联网重放（§12.2） |

### 14.2 实现

- 引擎通过 `TelemetryRepo.log(event)` 输出，本地先写内存环 + 可选 localStorage 落盘；
- 开发期提供调试面板（查看实际 DPS、经济曲线图）；
- 联网后可上报服务端做聚合分析（脱敏）；
- 操作流水同时作为 E 阶段重放校验的输入（§12.2），一鱼两吃。

---

## 15. 可扩展性总览（五大目标如何达成，修订措辞）

| 目标 | 机制 | 新增工作量（修订后如实表述） |
|---|---|---|
| 剧情可扩展 | 章节=JSON文件+manifest | 加文件，零代码 |
| 塔/敌人可扩展（数值） | Registry按id查+JSON配置 | 加条目，零代码 |
| 塔/敌人可扩展（新机制） | behavior/ability 注册表 | 加一个处理器，局部改动 |
| 平衡易调整 | 全数值在配置+平衡旋钮 | 改JSON |
| 前后端可分离（数据层） | Repository接口+依赖注入 | 换实现，引擎不动 |
| 前后端可分离（防作弊） | 确定性模拟+操作流水重放 | 现已埋好，E阶段启用 |
| 通用功能易扩展 | Modifier管线统一所有加成 | 加mod函数，结算逻辑不改 |

---

## 16. 开发阶段计划

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P1 核心骨架** | 网格棋盘+1路径+剑/弓/枪/聚灵阵4塔+妖狼/山猪2敌+单关3波+灵石血量+**固定步长主循环** | 能玩的MVP |
| **P2 数据驱动** | Registry+towers/enemies.json+behavior派发+波次编排+境界升级 | 配置化 |
| **P3 章节/剧情** | manifest+章节JSON+多路径分流+剧情面板+存档+解锁+星级 | 完整关卡流程 |
| **P4 成长系统** | Modifier管线(§9.5)+装备+皮肤+VIP+战斗规则(目标AI/加速/提前召唤) | 加成体系 |
| **P5 Meta+商业化** | Meta进度(§10)+IAPRepo+仙玉+月卡+寻仙抽卡 | F2P闭环（mock） |
| **P6 联网(C阶段)** | Repo换Remote+账号+服务端权威(仙玉/贡献)+战斗轻度校验+云存档 | 在线化 |
| **P7 遥测+打磨** | 遥测面板+数值平衡调参+特效+音效+更多关卡章节 | 上线级 |
| **P8(可选) 强防作弊** | E阶段：操作流水重放校验 | 反作弊 |

---

## 附录 A：核心数值速查

- 基准效率：1灵石 ≈ 5 单体 DPS
- 路径覆盖：射程2.5 ≈ 覆盖4格路径
- 护甲公式：伤害 × 100/(100+护甲)
- 赏金：独立配置（普通≈血量/10，高甲上浮，BOSS封顶转通关奖，分裂子体=0）
- 波次血量：基础 × (1 + 0.15×(N-1))
- 难度倍率：简单0.8 / 普通1.0 / 困难1.4 / 噩梦1.9
- Modifier 公式：`final = (base + Σadd) × (1 + Σmul_pct) × Πmul`，mul_pct 加法叠加；封顶按 stat 分级（伤害+150%/经济+80%/攻速射程+60%）

## 附录 B：待定决策（开发时确认）

- [ ] P1 先实装几种塔（建议：飞剑/符箓/长枪/聚灵阵 共4种）
- [ ] 五行克制 P1 是否开启（建议先关闭）
- [x] **利息机制**：默认关闭，启用则封顶 20灵石/波（N1/N7，已定档，见 §6.1）
- [ ] 联网后端技术栈（Node/Go/Serverless）
- [ ] 支付渠道（微信/支付宝/Steam）
- [ ] 是否走 E 阶段强防作弊（建议先 C 阶段上线，视作弊情况再上 E）
- [ ] Meta 天赋树是否在 P5 一并实装（建议先做软货币+抽卡，天赋后置）

## 附录 C：已否决功能（明确不实现）

> 以下功能经评估后决定永久搁置，不排入任何 backlog，不实现。

1. **键盘快捷键**（空格暂停、数字速度、QWER 选塔等）：产品形态为纯鼠标/触控点击，无快捷键需求，投屏后框架层屏蔽键盘事件。
2. **移动端触屏适配**（长按出售、响应式缩放）：定位为桌面端 Web 游戏，不考虑小屏适配。长按交互在塔防中易误触，响应式缩放会破坏固定网格布局。
