{
  "name": "xianxia-tower-defense",
  "version": "0.1.0",
  "description": "仙侠塔防 - P1 核心骨架",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n client,server \"vite\" \"npm run dev:server\"",
    "dev:client": "vite",
    "dev:server": "cd server && npx tsx watch index.ts",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^4.1.9",
    "concurrently": "^8.2.0"
  }
}
